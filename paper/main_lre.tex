\documentclass[pdflatex,sn-chicago]{sn-jnl}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{url}
\usepackage{xcolor} % required by \email (\authemail uses \textcolor)

\title[Public-Artifact Sufficiency Audit of the SLRTP2025 BT Evaluator]{Public-Artifact Sufficiency Audit of the SLRTP2025 Back-Translation Evaluator: A Failed Reconstruction and Descriptive Probe Analysis}

% LRE single-blind review: author information IS shown.
\author*[1]{\fnm{Xin} \sur{Ji}}
\author[1]{\fnm{Cheng} \sur{Zhang}}
\email{2024540101@bipt.edu.cn}
\affil*[1]{\orgname{Beijing Institute of Petrochemical Technology}, \orgaddress{\city{Beijing}, \country{China}}}

\begin{document}

\abstract{We audit whether the released SLRTP2025 back-translation (BT) evaluator for sign language production can be reconstructed from its public artifacts, an evaluation-only repository without training scripts. Our reconstruction attempt failed.

Twenty-eight recipe-conditioned runs---fourteen legacy-implementation seeds and fourteen near-faithful runs (four validation-freq-misread, ten step-corrected)---reach dev BLEU 6.5--9.8 versus 13.38, teacher-forced token accuracy 55--60\% versus 96.5\%, and training-pool readout 7.2--9.3 versus 78.8. Because all constructible checkpoints are less competent, we cannot distinguish whether the non-reproduction of a constructed adversarial replay-probe response ($+10.24$ sacreBLEU, post-selection descriptive interval) reflects the competence gap, an unrecovered training mechanism, or their interaction.

Reference replacement with human back-translations is associated with a gap reduction to $+0.95$ (from $+10.03$) on a 461-item subset---a single-annotator point estimate that cannot isolate reference-text defects from lexical-coupling disruption, paraphrase shift, or BLEU floor effects. Donor-pool substitution reverses the sign; a pool-size-controlled resampling (1{,}000 draws of 640-vs-640 donor pools) gives a residual origin effect of $+8.38$ BLEU (95\% interval $[+7.44,+9.34]$), an association rather than an identified training-exposure effect.

These findings constitute a failed public-artifact reconstruction, not a causal checkpoint effect or general leaderboard instability. We frame the probe as an adversarial lexical-coupling stress test throughout.}

\keywords{sign language production, back-translation evaluation, reproducibility audit, reference-frame sensitivity, learned evaluation metrics, PHOENIX-2014T}

\maketitle

\section{Introduction}

Sign language production (SLP) systems are commonly evaluated by back-translation (BT): a trained sign recognizer decodes each output pose sequence, and a text metric compares the decoding with the reference. This measures recognizer-readable content through a learned measurement model, so system rankings may reflect both the evaluated outputs and the inductive biases of a particular evaluator checkpoint. The present audit is framed as a \emph{public-artifact sufficiency audit}: whether the released SLRTP2025 BT evaluator can be reconstructed from its public artifacts. We address three research questions:

\begin{enumerate}
\item[RQ1.] Can the public artifacts reproduce the released evaluator's competence and training-pool readout?
\item[RQ2.] Does a frozen adversarial replay probe's ordering reproduce across evaluator checkpoints constructed from the public recipe?
\item[RQ3.] How sensitive is the probe response to the reference text and to donor origin?
\end{enumerate}

We do \emph{not} frame the work as a test of checkpoint identity. Every checkpoint we can construct from the public recipe is less competent than the released evaluator (14 paper-derived reconstructions: dev BLEU-4 6.5--9.4; 14 near-faithful runs: 8.3--9.8; broader constructible family training-log max 10.02; all vs.\ released 13.38), so competence and checkpoint identity are confounded. We therefore frame RQ2 strictly as a \emph{descriptive non-reproduction}: among independently constructed but uniformly less-competent checkpoints, the ordering does not reproduce. Without a competence-matched independent implementation---which we could not obtain---we cannot distinguish whether this reflects the competence gap, an unrecovered training mechanism, or their interaction. The replay probe itself is an \emph{adversarial lexical-coupling stress test}: the source-conditioned retriever selects donors by surface vocabulary overlap with the German source, which is identical to the scoring reference in PHX-public, so the probe deliberately targets the strongest form of the shortcut rather than a deployable retrieval pipeline.

Prior work shows BT scores can be modest for recorded signing and can improve without faithful target conditioning \cite{walsh2024datadriven,hong2026conditional} (Hong and Ko\v{s}eck\'{a}, arXiv preprint); sign-native or pose-aware metrics provide complementary evidence \cite{signbleu,silverscore,jiang2025meaningful}, yet BT remains central to SLRTP2025 \cite{slrtp2025}. Chen et al.~\cite{chen2022repro} showed that learned BERT-based evaluation metrics can fail to reproduce due to undocumented preprocessing, missing code, and baseline implementation differences---a methodology directly relevant to our finding that our public-artifact reconstruction attempt failed to reproduce the released checkpoint. An unresolved question is whether a retrieval--reference stress-test ordering persists across independently trained instances of the same BT architecture.

This question is exposed by an adversarial retrieval--reference probe on PHX-public: under the released evaluator, source-conditioned whole-donor retrieval scores 23.02 sacreBLEU (0--100 scale) versus 12.78 for the recorded test poses. Because the retriever selects donors by lexical overlap with the source sentence---which is identical to the scoring reference in PHX-public---this probe is a deliberately constructed adversarial stress test, not evidence of general evaluator preference or better signing.

We address the three research questions with the following evidence (full results in \S\ref{sec:positive_evidence}--\S\ref{sec:human_refs}). For RQ1, none of our reconstructions reached the released evaluator's competence. For RQ2, 43 of 44 non-degenerate constructible checkpoints give negative probe gaps (one step-corrected seed gives a small positive gap, $+0.24$); we cannot determine whether this reflects the competence gap or a checkpoint-identity effect. For RQ3, reference replacement with human back-translations is associated with a gap reduction; donor-pool substitution reverses the sign. The source-conditioned retriever shares surface vocabulary with the original reference (\S\ref{sec:retrieval}), so the RQ3 result conflates reference-text properties, lexical-coupling disruption, paraphrase shift, and BLEU floor effects.

\paragraph{Why a constructed probe matters.}
Retrieval-augmented SLP entries draw donors from the evaluator's training pool, so the adversarial probe isolates a risk that production systems face in diluted form. The audit contributes a transferable template (provenance table, checkpoint registry with SHA-256 deduplication, evaluator-replication controls, teacher-forced-vs-free-decode diagnostics) for public-artifact reproducibility testing. The specific probe response is a PHX-public released-evaluator case study and does not support general claims about BT evaluation or retrieval-based SLP.

\section{Related Work}

\paragraph{Learned evaluation in sign language production.}
Neural SLP often evaluates with a frozen sign recognizer \cite{camgoz2018neural,saunders2020progressive}; BT was proposed as an SLP evaluation protocol by Saunders et al.~\cite{saunders2021continuous}, who argued it shifts absolute scores but not relative rankings between similar configurations --- a proposition our audit does not directly test (its similarity condition is unmet in our family) but re-examines for a released evaluator on which a retrieval-probe reversal appears. SignBLEU \cite{signbleu}, SiLVERScore \cite{silverscore}, and pose-native measures offer complementary views, but BT BLEU remains central to SLRTP2025 \cite{slrtp2025}. Although newer metrics provide better human-correlated evaluation \cite{jiang2025meaningful,backtranslation2} (BackTranslation2.0, arXiv preprint), the SLRTP2025 BT evaluator is the \emph{standardized infrastructure} on which the challenge ranking depends; auditing its reproducibility and failure modes is infrastructure maintenance, not a claim that decoded BT-BLEU is the best available metric.

\paragraph{Retrieval and shortcut risks.}
Retrieval-based SLP ranges from phonetic composition to retrieval-enhanced generation \cite{walsh2024datadriven,signstitching,soke,darslp}, and SLRTP2025's leading entry is retrieval-based, making donor copying pertinent \cite{slrtp2025}. Memorization results in retrieval-augmented language modeling show strong task scores can reflect reproduction of training examples \cite{carlini2021extracting,kandpal2022memorization,khandelwal2020knnlm}; a shortcut under one learned evaluator does not show the ordering persists under another checkpoint.

\paragraph{Robustness of learned metrics and corpus audits.}
Hong and Ko\v{s}eck\'{a}~\cite{hong2026conditional} show BT-BLEU can diverge from target faithfulness; Walsh et al.~\cite{walsh2024datadriven} document a low recorded-pose BT baseline; Jiang et al.~\cite{jiang2025meaningful} recommend BT likelihood over decoded BLEU; and model-based generation metrics are further criticized by \cite{yazdani2025critical,backtranslation2,he2023blind}. Two 2026 PHOENIX-2014T audits bear directly on our reversal: Czehmann et al.~\cite{czehmann2026sacred} released human back-translations and documented reference information loss, Alkain et al.~\cite{alkain2026critical} showed the highest train--test text overlap among tested SLT corpora, and Artiaga et al.~\cite{artiaga2025signer} showed signer-dependent evaluation overestimates SLT capability. None runs multi-checkpoint evaluator experiments or donor-pool substitution controls.

\paragraph{Reproducibility of learned evaluation metrics.}
Chen et al.~\cite{chen2022repro} showed that reproducing BERT-based evaluation metrics can fail due to undocumented preprocessing, missing code, and baseline implementation differences, even when model weights are released---a finding methodologically close to our public-recipe non-reproduction result. The difference is that our audit targets a full BT evaluator pipeline (architecture, training recipe, and checkpoint) rather than a metric alone, and the non-reproduction is demonstrated across 44 non-degenerate independently trained instances (43 negative, one small positive).

\paragraph{PHOENIX-2014T as a language resource: known limitations.}
PHOENIX-2014T~\cite{camgoz2018neural} (8,257 weather-forecast interpretations by 9 signers) is the most widely used DGS parallel corpus, but four properties constrain our audit's generalizability: (i) template density inflates train--test n-gram overlap~\cite{alkain2026critical} (32 items whose reference exactly matches a training text account for a disproportionate share of the gap); (ii) reference texts are speech transcriptions, not DGS translations~\cite{czehmann2026sacred}; (iii) the 178-keypoint pose materialization discards handshape and non-manual detail (our MSKA control uses 133 keypoints; SI~Sup.~L); (iv) signer-dependent evaluation can overestimate capability~\cite{artiaga2025signer}. The lexical coupling between source and reference amplifies these effects in our adversarial probe. The transferable audit components (provenance table, checkpoint registry, leakage controls) apply to any learned evaluator paired with a public training corpus.

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{2pt}
\caption{Novelty relative to closely related PHOENIX-2014T and learned-metric reproducibility studies.}
\label{tab:novelty}
\begin{tabular}{p{3.0cm}p{3.3cm}p{3.3cm}p{4.0cm}}
\toprule
Study & Primary finding & Method & Distinction from present work \\
\midrule
Czehmann et al. (2026) & Reference replacement changes BLEU scores and rankings on PHOENIX-2014T & Human back-translations of test set & We apply the matched-subset protocol to the released evaluator with a frozen replay probe and quantify paired attenuation \\
Alkain et al. (2026) & Highest train--test text overlap among tested SLT corpora & N-gram overlap quantification & We use their finding to contextualize why PHX-public is particularly susceptible to reference coupling \\
Artiaga et al. (2025) & Signer-dependent evaluation overestimates SLT capability & Signer-disjoint splits & We include signer as a regression covariate (not signer-disjoint evaluation; $\Delta R^2$ from removing the signer block is $\leq1\%$) \\
Jiang et al. (2025) & BT likelihood is a more stable correlate of human judgment than decoded BLEU & Meta-evaluation of pose/embedding/BT metrics & We report that teacher-forced likelihood does not reproduce the reversal (REC NLL 3.060 vs PURE 3.479) \\
Chen et al. (2022) & BERT-based metrics fail to reproduce due to missing preprocessing/code & Multi-implementation replication & We extend the learned-metric reproducibility question to a full SLP evaluator pipeline with 78 training runs across 13 families \\
This work & Available public artifacts do not reproduce the released evaluator's competence or replay-probe behavior & 78 training runs across 13 families, fixed-output cross-evaluator re-decoding, reference/donor substitution & --- \\
\bottomrule
\end{tabular}
\end{table}

\section{Methods}
\label{sec:method}

\subsection{Evaluation design}
Let $E_c$ denote a BT evaluator checkpoint, $x_n$ the source sentence (German) for item $n$, and $o_{i,n}$ the pose sequence for output condition $i$ and item $n$; $E_c$ produces $h_{c,i,n}=E_c(o_{i,n})$ and $B_{c,i}=M(\{(h_{c,i,n},y_n)\}_{n=1}^{N})$, where $y_n$ is the reference text and $M$ is the corpus-level sacreBLEU functional unless stated otherwise. We call a result a \emph{descriptive non-reproduction} when a probe response observed under the released evaluator does not appear across independently constructed checkpoints under a fixed output set; this term is descriptive and does not imply a causal checkpoint-identity effect (which we cannot test because all constructible checkpoints are less competent). The fixed primary set comprises recorded test poses (REC), random donor copy, source-conditioned whole-donor retrieval, a Progressive Transformer generator, and retrieval--generator compositions; oracle-gloss variants are diagnostic controls.

\paragraph{Operational definition of public-artifact sufficiency.}
\label{sec:sufficiency_def}
A publicly documented training recipe is \emph{sufficient} when checkpoints constructed from it reach a predefined competence gate (our operational tolerance, not a community standard): dev BLEU-4 $\geq 12.38$ \emph{and} dev WER $\leq 86.37$ (one-sided). The gate conclusion is insensitive across a $[0.5,2.0]_{\text{BLEU}}\times[1.5,4.5]_{\text{WER}}$ grid (\S\ref{sec:what-drives-reversal}). The source of the gap may reflect information incompleteness, stochastic underspecification, or implementation bias; without the original training command, these cannot be separated.

The main evaluation uses PHX-public, a German weather-domain text-to-pose benchmark whose released SLRTP2025 pose records contain 7,060/515/641 sequences; one official test ID is absent from the released materialization, so every result is conditioned on the complete 641-record release (SI~Sup.~A). Table~\ref{tab:provenance} gives the three-way provenance: the official PHOENIX-2014T split, the released pose materialization, and the released evaluator's actual training manifest (confirmed via \texttt{config.yaml}, whose manifest matches the shipped \texttt{.pt} tensors). Supplementary analyses use \textbf{PHX-holdout-MSKA} (133-joint, 1,538 windows) and CSL-Daily (Mode B, 1,176 sequences; SI~Sup.~L); the protocol summary is in SI~Table~S28.

\begin{table}[t]
\centering
\scriptsize
\caption{Three-way data provenance for PHX-public. The released evaluator's training manifest matches the released pose materialization (7{,}060/515/641), not the full official PHOENIX-2014T split. All 41 missing IDs share the reason \texttt{absent\_from\_slrtp2025\_released\_pose\_records} (SI~Sup.~A). SHA-256 hashes verify file integrity.}
\label{tab:provenance}
\begin{tabular}{lrrp{3.5cm}}
\toprule
Split source & Train & Dev & Test \\
\midrule
Official PHOENIX-2014T & 7{,}096 & 519 & 642 \\
Released SLRTP2025 pose (\texttt{.pt}) & 7{,}060 & 515 & 641 \\
Released evaluator \texttt{config.yaml} manifest & 7{,}060 & 515 & 641 \\
\midrule
Missing from official & 36 & 4 & 1 \\
\bottomrule
\end{tabular}
\end{table}

All pose sequences are evaluated at 12.5\,fps, matching the SLRTP2025 evaluator training distribution. The data flow is: official PHOENIX-2014T video at 25\,fps $\to$ SLRTP2025 skeleton extraction at 25\,fps $\to$ our \texttt{[::2]} subsampling to 12.5\,fps for evaluator input; the official evaluation command uses \texttt{-{}-fps 25} (the input file's native rate), but the evaluator was trained on 12.5\,fps-subsampled poses, so our subsampling matches the training distribution rather than the command-line flag. A three-path regression confirms no double subsampling (details in SI~Sup.~F). PHX-public uses $\alpha{=}0.88$ (selected on dev); the supplementary splits use $\alpha{=}0.5$.

\subsection{Systems and retrieval controls}
\label{sec:retrieval}
For each target, source-conditioned whole-donor retrieval scans the complete training pool after Unicode NFKC normalization, whitespace normalization and lowercasing, selecting the sequence with maximum source-text token-set Jaccard similarity. Oracle-gloss retrieval instead uses target gloss and is treated as an input-leakage diagnostic; random retrieval selects a donor uniformly. Donor exclusion removes exact normalized text or gloss duplicates or donors above threshold $\tau$; gloss-based exclusion is therefore an offline diagnostic, not a deployable inference procedure.

\emph{Single canonical materialization.} Every number in this paper is generated from one canonical donor registry built by the deterministic \S\ref{sec:retrieval} algorithm (exact-normalized-text exclusion applied), whose SHA-256 is \texttt{9170a53026ab3263b451a3632a9e02\-318745acabf12f0b679921477afbefa301}. Reproduction is supported at four layers (\S~Sup.~Q, Table~S36): (i) \textbf{L1---recompute selected reported statistics}: a single command (\texttt{make core-audit}) regenerates the registry and verifies headline numbers from committed per-item decodes; it does \emph{not} retrain models or decode from checkpoints, and does not verify every number in the paper; (ii) \textbf{L2---analysis re-execution}: each analysis script re-executes from the committed per-checkpoint data and hypotheses; (iii) \textbf{L3---model re-evaluation}: re-decoding fixed outputs from checkpoint weights requires the training bundle; (iv) \textbf{L4---from-scratch retraining}: full retraining requires the restricted PHOENIX-2014T licence, the exact environment, and approximately 200 GPU-hours. The \texttt{make core-audit} command executes L1 only and is not an end-to-end reproduction. A claim-to-file manifest mapping each headline number to its source file and verification command is in \texttt{artifact/claim\_manifest.json}. An earlier pre-exclusion materialization (PURE 23.79, gap $+11.01$) survives only as a sensitivity analysis in SI~Sup.~N.

\paragraph{Lexical coupling of the source-conditioned probe (adversarial design).}
In PHX-public, the source sentence $x_n$ (German weather report) is identical to the reference text $y_n^{\text{orig}}$ at the sequence level (the reference is a speech transcription of the same German sentence). The TN-PURE-v1 retriever selects training-pool donors by token-set Jaccard similarity between $x_n$ and training texts, so it preferentially retrieves donors whose accompanying training texts share surface-level vocabulary with both the source \emph{and} the reference. Scoring a donor copy against this lexically coupled reference under a learned evaluator therefore confounds donor similarity, evaluator readout, and reference overlap. We explicitly frame this probe as an \emph{adversarial stress test} targeting the strongest form of the lexical-coupling shortcut---not as evidence of general evaluator preference, deployable retrieval quality, or better signing. This framing has two consequences: (i) the $+10.24$ gap is the expected output of a deliberately adversarial design, not a discovery about the evaluator's ranking behavior; (ii) the human-reference attenuation analysis (\S\ref{sec:human_refs}) reflects disruption of the lexical coupling as well as reference-text information loss, paraphrase shift, and BLEU floor effects---not a clean separation of any single mechanism.

\paragraph{Retrieval composition.}
\label{sec:composition}
TN-PURE-v1 uses whole-donor copy with [::2] subsampling to 12.5\,fps, preserving the donor's original duration. TN-PTCOMP-v1 (a secondary control) composes a PT scaffold with donor hands and face via $\alpha$-blending of upper-body joints ($\alpha{=}0.88$; full joint-set definitions in the artifact). The matched seen--unseen factorial retains 605/641 targets matched on duration and word count.

\paragraph{Donor-origin contrast retrieval systems.}
\label{sec:exposure_systems}
To test whether the probe response co-occurs with donor origin, we construct frozen probe systems: \textbf{UNSEEN-PURE-v1} retrieves from the 640 other test poses; \textbf{CROSSFIT-PURE-A/B} split queries by hash into two folds; \textbf{SEEN-PURE-MATCHED-v1} selects a train-pool donor within $\pm0.1$ Jaccard of each UNSEEN donor (622/641 admit one). SEEN-PURE-v1 re-uses the canonical TN-PURE-v1 registry; these are constructed probes, not deployable generators (construction rules in the artifact).

\subsection{Evaluator checkpoint family}
The main inferential checkpoint family comprises the original frozen SLRTP2025 evaluator and six \emph{recipe-conditioned reconstruction} runs trained from scratch with the documented architecture on a common frozen partition (six prospectively specified legacy seeds 101--606; eight extension seeds 707--1405 use the identical protocol). The original bundle's \texttt{validations.txt} logs 202 validation points over 2{,}828 optimizer steps ($\approx$102 epochs), with decoded-BLEU selection reaching best dev BLEU-4 13.38 at step 1{,}820; the trajectory is analyzed in \S\ref{sec:original_trajectory}, and intermediate checkpoints are not published. We use two reconstruction protocols --- \emph{paper-derived} (14 runs: 6 prospectively specified seeds 101--606, 8 extension seeds 707--1405; legacy script, 300 epochs, NLL selection) and \emph{near-faithful} (14 runs: 4 validation-freq-misread $+$ 10 step-corrected; decoded-BLEU early stopping) --- sharing the documented architecture (3L/256/512ff, 8 heads), tokenizer, manifests (7{,}060/515), $[::2]$ subsampling, Adam (1e-3, wd .001), batch size 256, and plateau $\times0.8$ LR schedule (full provenance in SI). In total 78 from-scratch training runs across 13 families (registry in SI~Sup.~D); 49 were beam-3 decoded on PHX-public under the canonical \S\ref{sec:retrieval} donor registry, corresponding to 48 unique weight binaries --- one validation-freq-misread seed-202 artifact is byte-identical to a long-schedule artifact (same wandb run, identical per-step losses and best-checkpoint SHA-256). Of the 49 decoded training runs, five are degenerate (three $\alpha{=}1.0$ distillation students with empty hypotheses, two smallest ladder fractions with BLEU${}\approx 0$) and are excluded, so the non-replication result rests on 44 non-degenerate decoded training runs (43 with strictly negative gaps; one step-corrected seed gives a small positive gap of $+0.24$). Table~\ref{tab:accounting} gives the full accounting.

\paragraph{What the 78 runs are---and are not.}
\label{sec:provenance_table}
The 78 runs are \emph{not} 78 same-recipe replication attempts. Table~\ref{tab:public_provenance} maps each family to the public source it draws on, the known deviation from the original training, the number of runs, the checkpoint-selection rule, and the resulting dev BLEU-4 range. The 10 step-corrected runs and the 4 validation-freq-misread runs approach a faithful reconstruction of the released recipe; the remaining 64 runs are diagnostic, sensitivity, holdout, or competence-extrapolation analyses. The public-recipe non-reproduction conclusion rests on the 14 near-faithful runs (of which 10 are fully step-corrected), while the remaining families serve as triangulating evidence that no alternative construction we tested reproduces the reversal.

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{2pt}
\caption{Public-artifact provenance of each reconstruction family. ``Dev BLEU-4'' is the uniform full-pool beam-3 value (\S\ref{sec:sanity_checks}); the released evaluator scores 13.38. ``Selection'' is the checkpoint-selection rule. The 14 paper-derived runs use NLL selection on a legacy script; the validation-freq-misread runs originally misread \texttt{validation\_freq=14} as epochs before the step-corrected correction. Only recipe-faithful families support the public-recipe non-reproduction claim; the rest are diagnostic.}
\label{tab:public_provenance}
\begin{tabular}{p{2.2cm}p{3.0cm}p{3.5cm}p{0.8cm}p{1.5cm}p{1.2cm}}
\toprule
Family & Public source used & Known deviation & Runs & Selection & Dev BLEU-4 \\
\midrule
Paper-derived (prosp.-spec. + ext.) & config.yaml + slt architecture & legacy script; 300 epochs; NLL selection & 14 & NLL on dev & 6.5--9.4 \\
Validation-freq-misread & config.yaml (decoded-BLEU) & \texttt{validation\_freq=14} misread as epochs initially & 4 & decoded BLEU & 8.3--9.8 \\
Step-corrected & corrected step counting & fixed \texttt{validation\_freq} interpretation & 10 & decoded BLEU & 7.6--9.4 \\
Confirmation & recipe-faithful protocol & independent re-run for reproducibility & 2 & decoded BLEU & 9.0--9.5 \\
Long-schedule & config.yaml (epochs=3000) & extended training budget & 2 & decoded BLEU & $\leq$10.02 \\
Rescue sweep & hyperparameter grid & dropout/wd/lr/batch variations; none passed gate & 20 & decoded BLEU & $\leq$10.02 \\
Distillation & released checkpoint as teacher & student--teacher KL; inherits released weights & 15 & decoded BLEU & 7.0--9.0 \\
Ladder (data fractions) & config.yaml & data-subset fractions (0.125--0.75$\times$) & 4 & decoded BLEU & $\leq$9.8 \\
Large-arch & scaled architecture & wider/deeper variants & 4 & --- & not decoded \\
Holdout controls & cross-fit / BT-retrained & holdout splits only & 3 & --- & not decoded \\
\midrule
\textbf{Total} & & & \textbf{70} & & \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Training-pool readout (descriptive observation).}
\label{sec:readout_def}
We define \emph{training-pool readout} as the in-sample autoregressive free-decode BLEU and exact-match (EM) rate on the full 7{,}060-item training set under beam-3 decoding---not teacher-forced accuracy. The released evaluator decodes its full 7{,}060-item training pool at free-decode BLEU 78.8 with 70.7\% exact-match, while retaining dev/test performance of 13.38/12.78. The constructible family's readout is far lower (reconstructions: 7.2--9.3 BLEU, 1.5--2.3\% EM; distillation: 6.6--8.9 BLEU, 1.3--1.9\% EM); no constructible checkpoint approaches both simultaneously. The training-pool free-decode BLEU ratio (released: $\text{78.8}\,/\,\text{13.38}\approx5.9$ vs.\ reconstruction family: 0.93--1.12) quantifies how much the model reads out above its in-domain performance. We report these as continuous descriptive observations: the released evaluator was observed first, the family range afterwards, and any threshold is post-hoc.

\subsection{Reconstruction sanity checks}
\label{sec:sanity_checks}
Because the 28 recipe-conditioned runs (14 paper-derived $+$ 14 near-faithful) share the same codebase, data pipeline, and evaluation protocol, we performed three checks on them to rule out a systematic implementation defect (the 47 diagnostic runs---distillation, rescue, ladder, long-schedule, large-arch---and 3 holdout controls deliberately vary the objective, split, architecture, or hyperparameters, and are not claimed to share it):

\begin{enumerate}
\item[\textbf{Small-sample overfit test.}] A reconstruction model trained on 50 and 100 sequences overfits to free-decode BLEU 100.00 (NLL $\to$ 0.009), ruling out defects in loading, masking, or decoding (\texttt{results/overfit\_test.json}).
\item[\textbf{Unified inference regression.}] Released and reconstruction checkpoints load through the identical pipeline with byte-identical vocab files (SHA-256 confirmed); the released checkpoint reproduces canonical REC 12.78 exactly.
\item[\textbf{Teacher-forced vs.\ free-decode.}] Table~\ref{tab:sanity_stats} shows the released evaluator achieves 96.5\% teacher-forced train token accuracy and 78.8 free-decode train BLEU, while reconstructions achieve 55--60\% and 7.2--9.3. The gap is present in both teacher-forced and free-decode metrics on both train and dev sets.
\end{enumerate}

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{2pt}
\caption{Teacher-forced and free-decode diagnostic statistics for the released evaluator and the fourteen reconstruction seeds (full table in \texttt{results/checkpoint\_stats.json}). Teacher-forced values are on 500 randomly sampled items (seed 42, sampled without replacement; the sampling SD across 5 re-samples is $\leq$0.3 pp for token accuracy and $\leq$0.01 for NLL/tok); free-decode values are full-pool (7{,}060 train / 515 dev) under the uniform beam-3 protocol (\S\ref{sec:sanity_checks}). sacreBLEU \texttt{signature: BLEU|c|n:4|e:no|tok:13a|s:exp|v:2.5.1}.}
\label{tab:sanity_stats}
\begin{tabular}{lrrrrr}
\toprule
Checkpoint & Train tok.\ acc.\ & Train NLL/tok & Train free-decode BLEU & Dev NLL/tok & Dev free-decode BLEU \\
\midrule
Released & 0.965 & 0.158 & 78.82 & 3.236 & 13.38 \\
Reconstructions (range) & 0.550--0.596 & 1.72--1.92 & 7.20--9.25 & 2.63--2.68 & 6.49--9.37 \\
Reconstructions (mean) & 0.572 & 1.82 & 8.35 & 2.66 & 8.13 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Training-implementation diagnostics.}
\label{sec:training_diag}
The reconstruction training logs (\texttt{checkpoints/*/training\_log.json}, $n{=}43$) record per-epoch train/dev NLL and learning rate. All recipe-conditioned runs use effective batch size 256 (physical batch 64 $\times$ gradient accumulation 4), Adam (1e-3, weight decay 1e-3), and the plateau $\times0.8$ schedule on dev NLL, with identical config (\texttt{config\_sha256} \texttt{0321104a\ldots}) and byte-identical vocabularies to the released evaluator. The median train-NLL trajectory is healthy (epoch 1: 5.83; epoch 5: 2.99; epoch 10: 2.54; epoch 25: 2.09 across 28 reconstruction logs), confirming normal optimization. Per-step loss, gradient norm, and per-epoch teacher-forced token accuracy are not archived (only per-epoch NLL aggregates were logged); we therefore cannot rule out a subtle scheduler-timing or normalization difference, which is consistent with our scoped ``failed reconstruction attempt'' framing. A 25$\to$12.5\,fps pose-subsampling regression test (\texttt{scripts/test\_fps\_regression.py}) passes on 8 released pose tensors, ruling out double subsampling. Full diagnostics in \texttt{results/robustness\_diagnostics.json} and \texttt{results/fps\_regression\_test.json}.

\subsection{Discovery timeline and pre-registration status}
\label{sec:discovery}
The $+10.24$ probe response was first observed on 2026-07-24 (canonical materialization; earlier pre-exclusion gap $+11.01$ on 2026-07-22). The six prospectively specified legacy seeds (101--606) were specified before their results were inspected; the eight extension seeds (707--1405) were added post-hoc. The Jaccard retriever, PURE protocol, and 641-query set were locked before reconstruction training began, after exploring the probes in Table~\ref{tab:probe_multiplicity}. No external pre-registration exists; these six seeds constitute the only pre-specified confirmatory evidence. Because the Jaccard pure-copy probe was selected as the maximal-gap probe post-hoc, its CI is descriptive, not confirmatory. Full provenance in SI~Sup.~C.

\subsection{Metrics and uncertainty}
The recorded test poses (REC) are decoded by each BT recognizer; we avoid the abbreviation ``ground truth'' because recorded poses are inputs to a learned measurement model, not a semantic gold standard. We report corpus BLEU-4 on the 0--100 sacreBLEU scale \cite{post2018sacrebleu} (signature: \texttt{BLEU|c|n:4|e:no|tok:13a|s:exp|v:2.5.1}). BLEU-1, chrF, ROUGE-L, BERTScore \cite{zhang2020bertscore}, and WER (jiwer 3.1.0) provide cross-metric consistency checks (not independent semantic validation, as all share decoded outputs). BT likelihood and pose-DTWp provide non-decoded controls (SI~Sup.~O).

Uncertainty is quantified with donor-cluster bootstrap (primary; 10{,}000 resamples) and item-level bootstrap (SI~Sup.~G). A pairwise difference is called detectable when its interval excludes zero. A descriptive seed-level 95\% Student-$t$ prediction interval is reported for the legacy-implementation family only; its interpretation is ``given a future random seed under this identical implementation,'' not a population-level claim. The six primary seeds give PI $[-2.48,-0.28]$; all 14 leave-one-seed-out folds keep the interval below zero (SI~Sup.~B). The headline gap is a \textbf{post-selection descriptive estimate} (\S\ref{sec:probe_multiplicity}); all other contrasts are Holm-corrected within a defined family or explicitly labelled descriptive (SI~Sup.~C).

\subsection{Terminology: run, checkpoint, unique binary, family, evaluator}
Throughout, \emph{run} denotes a single training execution; \emph{checkpoint} denotes the selected model weights from that run; \emph{unique weight binary} counts distinct SHA-256 hashes of checkpoint files (the cf\_202 and ls\_202 runs produced byte-identical weights from the same wandb run); \emph{family} denotes a group of runs sharing a training protocol; and \emph{evaluator} denotes a checkpoint used to decode fixed outputs. The 14 paper-derived reconstruction runs use NLL selection on the legacy script; the 4 released-config runs use decoded-BLEU early stopping. Both share the documented architecture and hyperparameters (\S\ref{sec:method}).

\paragraph{Custom-terminology registry.}
Table~\ref{tab:terminology} maps each author-defined term to its estimand, dataset, and evidence grade, following the reviewer's request. Evidence grade: D = descriptive (no formal error control); all custom quantities in this paper are descriptive.

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{3pt}
\caption{Custom-terminology registry. Each term maps to its estimand, the dataset on which it is computed, and its evidence grade (all descriptive).}
\label{tab:terminology}
\begin{tabular}{p{3.0cm}p{4.0cm}p{3.5cm}p{1.5cm}}
\toprule
Term & Estimand & Dataset & Grade \\
\midrule
Training-pool readout & In-sample autoregressive free-decode BLEU/EM & Full 7{,}060-item training set & D \\
PURE--REC gap & Corpus sacreBLEU-4 difference (PURE $-$ REC) & 641-item PHX-public test & D (post-selection) \\
Operational tolerance gate & One-sided: dev BLEU-4 $\geq$ 12.38 \emph{and} dev WER $\leq$ 86.37 & 515-item dev set & D \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Claim hierarchy.}
\label{sec:claim_hierarchy}
We classify every claim in this paper into four evidence tiers:
\begin{itemize}\itemsep0pt
\item \emph{Descriptive (D)}: observed pattern with no formal error control. Most findings in this audit are descriptive.
\item \emph{Sensitivity (S)}: the result is qualified by an explicit post-hoc condition (e.g., single-annotator, matched subset, post-selection).
\item \emph{Confirmatory (C)}: pre-specified hypothesis with formal error control. Only the six prospectively specified legacy seeds (101--606) qualify, for the non-reproduction direction.
\item \emph{Not identified (NI)}: the claim cannot be made from available data. The causal checkpoint-identity effect is NI because competence is confounded.
\end{itemize}

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{3pt}
\caption{Claim hierarchy. Each key claim mapped to its evidence tier and supporting evidence.}
\label{tab:claim_hierarchy}
\begin{tabular}{p{3.5cm}p{1.0cm}p{5.0cm}}
\toprule
Claim & Tier & Evidence \\
\midrule
Public artifacts do not reproduce released evaluator competence & D & 28 recipe-conditioned runs, all dev BLEU $<$ 10.02 \\
Probe response does not reproduce across weaker checkpoints & D & 43/44 non-degenerate gaps negative \\
Probe response reflects a causal checkpoint-identity effect & NI & Competence confound: 6.5--9.8 vs 13.38 \\
Headline gap $+10.24$ is post-selection & S & Max-gap probe among 8 explored; CI descriptive \\
Reference replacement attenuates the gap (90.5\%) & S & Single-annotator, 461-item, 4 confounded changes \\
Origin effect persists under matched pools ($+9.56$) & S & 148 items, SMD$<$0.1; association not causal \\
Probe is adversarial lexical-coupling stress test & D & Source $=$ reference in PHX-public; retriever uses source Jaccard \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Experimental stratification.}
\label{sec:stratification}
We classify every checkpoint family into four evidential tiers. To avoid overloading the word ``primary'', we distinguish \emph{prospectively specified} seeds (the only confirmatory evidence) from \emph{near-faithful} runs (the closest recipe match, but specified post-hoc):
\begin{itemize}\itemsep0pt
\item \emph{Near-faithful (corrected-recipe)}: 10 step-corrected $+$ 4 validation-freq-misread $=$ 14 runs. The 10 step-corrected runs match the released validation schedule exactly; the 4 validation-freq-misread runs initially read \texttt{validation\_freq=14} as 14 epochs before the step-corrected correction, but both groups use decoded-BLEU selection as the released recipe does. These are the closest recipe matches and the primary recipe-faithfulness evidence for the reconstruction-failure claim.
\item \emph{Secondary (legacy-implementation replication)}: 14 paper-derived seeds (6 prospectively specified seeds 101--606 $+$ 8 extension seeds 707--1405; legacy script, 300 epochs, NLL selection). The 6 prospectively specified seeds are the only \emph{confirmatory} evidence (pre-specified non-reproduction direction); the family shares a known deviation from the released protocol (legacy script, NLL selection) and serves as supplementary stability evidence.
\item \emph{Diagnostic (post-hoc sensitivity)}: 2 confirmation $+$ 20 rescue $+$ 15 distillation $+$ 4 ladder $+$ 2 long-schedule $+$ 4 large-arch $=$ 47 runs. These probe mechanism and competence extrapolation.
\item \emph{Holdout controls}: 2 cross-fit $+$ 1 BT-retrained $=$ 3 runs, evaluated on holdout splits only.
\end{itemize}
The reconstruction-failure claim rests on the 14 near-faithful runs (10 step-corrected, 4 validation-freq-misread; descriptive step-corrected 10-seed prediction interval $[-1.89,+0.45]$, 9/10 negative) together with all 14 legacy seeds; the \emph{confirmatory} non-reproduction direction comes only from the 6 prospectively specified legacy seeds (101--606, prediction interval $[-2.48,-0.28]$, all six leave-one-seed-out folds below zero). Diagnostic runs serve as triangulating context and must not be cumulated with confirmatory evidence to inflate the appearance of independent replication. Table~\ref{tab:accounting} gives the full accounting, generated from a single machine-readable registry (\texttt{results/accounting\_table.json}).

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{3pt}
\caption{Checkpoint accounting, auto-generated from \texttt{results/accounting\_table.json} (single source of truth). ``Runs'' = from-scratch training executions; ``Decoded'' = beam-3 decoded on the canonical PHX-public gap panel; ``Unique'' = distinct SHA-256 weight binaries; ``Non-deg.'' = non-degenerate (BLEU $>$ 0, excluding 3 $\alpha{=}1.0$ distillation students with empty output and 2 smallest ladder fractions). The near-faithful tier (6 corrected-recipe runs) provides recipe-faithfulness evidence; secondary (14 legacy, of which 6 prospectively specified) provides the confirmatory non-reproduction direction plus stability; diagnostic and holdout are mechanism exploration.}
\label{tab:accounting}
\begin{tabular}{p{2.8cm}rrrr}
\toprule
Tier / Family & Runs & Decoded & Unique & Non-deg. \\
\midrule
\textbf{Near-faithful} & 14 & 14 & 14 & 14 \\
\quad Validation-freq-misread & 4 & 4 & 4 & 4 \\
\quad Step-corrected & 10 & 10 & 10 & 10 \\
\textbf{Secondary} & 14 & 14 & 14 & 14 \\
\quad Paper-derived (prospectively specified) & 6 & 6 & 6 & 6 \\
\quad Paper-derived (extension seeds) & 8 & 8 & 8 & 8 \\
\textbf{Diagnostic} & 47 & 21 & 20$^*$ & 16 \\
\quad Confirmation & 2 & 2 & 2 & 2 \\
\quad Rescue (dropout/wd variants) & 12 & 1 & 1 & 1 \\
\quad Rescue (lr variants) & 8 & 0 & 0 & 0 \\
\quad Distillation & 15 & 13 & 13 & 10 \\
\quad Ladder (data fractions) & 4 & 4 & 4 & 2 \\
\quad Long-schedule & 2 & 1 & 0$^*$ & 1 \\
\quad Large-arch & 4 & 0 & 0 & 0 \\
\textbf{Holdout controls} & 3 & 0 & 0 & 0 \\
\midrule
\textbf{Total trained} & \textbf{78} & \textbf{49} & \textbf{48} & \textbf{44} \\
\bottomrule
\end{tabular}
\par\smallskip
{\footnotesize $^*$One validation-freq-misread seed-202 artifact is byte-identical to a long-schedule artifact (same wandb run); it counts once in the unique-binary column. The released evaluator (1 checkpoint) is not a training run and is not included above. Eleven released-weight fine-tune variants (Gaussian noise, local perturbation) are not from-scratch training and are not counted here.}
\end{table}

\section{Results}
\subsection{Adversarial probe response and conditioning controls}
\label{sec:public-diagnostic}
Under the released SLRTP2025 evaluator, source-conditioned whole-donor retrieval scored 23.02 sacreBLEU (0--100 scale), compared with 12.78 for the recorded test poses---a gap of $+10.24$ BLEU points (95\% CI $[+8.87,+11.64]$; 10,000 donor-cluster resamples; item-level bootstrap gives the nearly identical $[8.88, 11.62]$). This is the expected output of a deliberately adversarial lexical-coupling stress test (\S\ref{sec:retrieval}): the retriever selects donors by surface vocabulary overlap with the source, which is identical to the scoring reference, so the gap measures the evaluator's susceptibility to this specific shortcut rather than general retrieval quality or signing adequacy. A deployment-realistic noisy-gloss retrieval pipeline scores only $-0.13$ relative to REC under the same evaluator (\S\ref{sec:competition_real}), confirming that the gap is specific to the constructed adversarial probe.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{generated_figures/fig2_headline.pdf}
\caption{Headline reversal under the released evaluator and its absence across recipe reconstructions. (a) System scores under the released evaluator (sacreBLEU-4, canonical 641-query protocol): TN-PURE 23.02 vs.\ REC 12.78 ($\Delta=+10.24$, 95\% CI $[8.87,11.64]$), with TN-PTCOMP 18.86, PT 1.59, oracle-gloss PURE 11.4, and random-donor PURE 0.90 (seeds 0--19, $\pm$1 SD). (b) Per-checkpoint PURE$-$REC gaps: released evaluator ($+10.24$) vs.\ fourteen recipe reconstructions, all negative (donor-cluster bootstrap 95\% CIs; seed-level 95\% PI $[-1.94,-0.46]$). All reconstructions are less competent than the released evaluator; the figure demonstrates public-pipeline non-replication, not an isolated causal checkpoint effect.}
\label{fig:headline}
\end{figure}

Controls localized the observation to source-conditioned donor retrieval (SI~Table~S22): pure donor copy (23.02) exceeded PT-composed (18.86), oracle-gloss (11.4--11.8, a leakage diagnostic), PT baseline (1.59), and 20-seed random-donor baselines (0.90/0.77). Source-text Jaccard was the strongest retriever (vs.\ LaBSE 19.2, multi-SBERT 16.5). Non-decoded controls do not reproduce the gap: teacher-forced NLL gives REC 3.060 vs PURE 3.479, and pose-DTWp (pose-sequence dynamic time warping distance, lower is better; near zero means the output pose sequence is close to the reference pose) is near zero for all non-REC conditions, confirming the decoded-text gap is not mirrored at the pose level. Complementary text metrics (BLEU-1, chrF, ROUGE-L, WER, BERTScore) agree on direction (SI~Table~S22); decoding-parameter sweeps (beam 1/3/5, length penalty, max length) leave the gap at $+9.9$ to $+10.8$ (SI~Sup.~F).

\paragraph{Probe multiplicity and post-selection status of the headline gap.}
\label{sec:probe_multiplicity}
Before freezing the headline probe, we explored four retrieval strategies (source-text Jaccard, LaBSE, multi-SBERT, oracle gloss) crossed with three construction variants (pure copy, PT-composed, oracle-gloss-composed) on the released evaluator alone. Of the 12 retrieval--construction cells, 6 were explored, 3 are not applicable (oracle-gloss-composed is definitionally tied to the oracle-gloss retriever, so it cannot be applied to Jaccard/LaBSE/multi-SBERT without changing the retriever), and 3 were not explored (LaBSE$\times$PT-composed and multi-SBERT$\times$PT-composed were deemed low priority after Jaccard pure-copy dominated; oracle-gloss$\times$oracle-gloss-composed is subsumed by oracle-gloss$\times$PT-composed). We additionally explored two random-donor baselines that lie outside the $4\times3$ matrix (Table~\ref{tab:probe_multiplicity} and \texttt{results/probe\_multiplicity.json}). The headline Jaccard pure-copy probe was selected as the \emph{maximal-gap} cell among the explored cells; its 95\% CI $[+8.87,+11.64]$ is therefore \textbf{post-selection descriptive}. We do not apply a multiplicity correction: the family of explored probes was defined after observing the gaps, so no correction would make the result confirmatory. The six prospectively specified legacy seeds (\S\ref{sec:discovery}) were pre-specified and provide the only confirmatory evidence (for the non-reproduction direction, not for the gap magnitude).

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{3pt}
\caption{Complete retrieval $\times$ construction design matrix explored on the released evaluator. Scores are corpus-level sacreBLEU-4 (0--100) on the canonical 641-item PHX-public set (REC$=$12.78). Of the 12 retrieval--construction cells, 6 were explored, 3 are not applicable ($^a$), and 3 were not explored ($^*$, $^\dagger$); full reasons in \texttt{results/probe\_multiplicity.json}. The headline probe (bold) was selected as the maximal-gap explored cell; its CI is post-selection descriptive. Two random-donor baselines (below the midrule) lie outside the $4\times3$ matrix.}
\label{tab:probe_multiplicity}
\begin{tabular}{lllrr}
\toprule
Retriever & Construction & Explored & Score & Gap vs.\ REC \\
\midrule
\textbf{Source-text Jaccard} & \textbf{Pure donor copy} & \checkmark & \textbf{23.02} & $\mathbf{+10.24}$ \\
Source-text Jaccard & PT-composed & \checkmark & 18.86 & $+6.08$ \\
Source-text Jaccard & Oracle-gloss-comp. & --- & --- & N/A$^a$ \\
LaBSE & Pure donor copy & \checkmark & 19.2 & $+6.42$ \\
LaBSE & PT-composed & --- & --- & not explored$^*$ \\
LaBSE & Oracle-gloss-comp. & --- & --- & N/A$^a$ \\
multi-SBERT & Pure donor copy & \checkmark & 16.5 & $+3.72$ \\
multi-SBERT & PT-composed & --- & --- & not explored$^*$ \\
multi-SBERT & Oracle-gloss-comp. & --- & --- & N/A$^a$ \\
Oracle gloss & Pure donor copy & \checkmark & 11.4 & $-1.38$ \\
Oracle gloss & PT-composed & \checkmark & 11.8 & $-0.98$ \\
Oracle gloss & Oracle-gloss-comp. & --- & --- & not explored$^\dagger$ \\
\midrule
\multicolumn{5}{l}{\emph{Random-donor baselines (outside the $4\times3$ matrix)}} \\
Random donor & Pure donor copy & \checkmark & 0.90 & $-11.88$ \\
Random donor & PT-composed & \checkmark & 0.77 & $-12.01$ \\
\bottomrule
\end{tabular}
\par\smallskip
{\footnotesize $^a$Oracle-gloss-composed is definitionally tied to the oracle-gloss retriever, so it cannot be applied to a text-based retriever without changing the retriever. $^*$Not explored; deemed low priority after Jaccard pure-copy dominated. $^\dagger$Subsumed by Oracle-gloss $\times$ PT-composed. Of the 12 retrieval--construction cells, 6 were explored, 3 are not applicable, and 3 were not explored. The headline probe was selected as the maximal-gap cell, so its CI is post-selection descriptive.}
\end{table}



The probe reversal motivates the prior question: can the public recipe reproduce the released evaluator's competence at all?

% --- RQ1 moved before RQ2 for logical dependency (reviewer round-4) ---

\subsection{RQ1: Failed public-artifact reconstruction---what the released evaluator has that reconstructions lack}
\label{sec:positive_evidence}
The non-reproduction in RQ2 raises the prior question of RQ1: can the public recipe reproduce the released evaluator's competence? We approach it through three descriptive comparisons (Fig.~\ref{fig:competence}b,c; Table~\ref{tab:diagnostic_summary}; full version in SI~Sup.~D), reporting them as descriptive triangulation, not three independent tests.

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{3pt}
\caption{Summary of the three primary descriptive comparisons. The released evaluator sits far outside the constructible family range on all three views. Family $n$: (i)~49 beam-3-decoded training runs (range over 44 non-degenerate; 5 degenerate excluded); (ii)~30 constructible checkpoints with uniform full-pool readout (released listed separately); (iii)~52 gate-eligible non-distillation training runs. Per-view family composition in SI~Sup.~D.}
\label{tab:diagnostic_summary}
\begin{tabular}{p{3.0cm}p{1.0cm}p{2.8cm}rr}
\toprule
Comparison & Family $n$ & Released evaluator & Family range & Released $-$ family max \\
\midrule
(i) Gap--competence & 49 & $+10.24$ at dev 13.38 & $[-2.01, +0.24]$ (44 non-deg.) & $+10.45$ \\
(ii) Train-pool readout BLEU & 30 & 78.8 (70.7\% EM) & 6.6--9.3 (1--2\% EM) & $+69.5$ \\
(iii) Competence gate & 52 & 13.38 & 6.5--9.8 (uniform; training-log max 10.02) & $+3.55$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Readout \emph{with} generalization: the distinctive combination.}
\label{sec:insample}
The released evaluator decodes its full 7{,}060-item training pool at free-decode BLEU 78.8 with 70.7\% exact-match while retaining dev/test performance of 13.38/12.78. The constructible family stays far below: reconstructions span train BLEU 7.2--9.3 at dev BLEU 6.5--9.4, and distillation students 6.6--8.9 at dev 7.0--9.0. Across 31 checkpoints with uniform full-pool readouts (30 constructible plus released), training-pool readout tracks decoded competence (Spearman $\rho{=}0.916$, $n{=}31$; $\rho{=}0.907$ with the released evaluator excluded, $n{=}30$, confirming the correlation is not driven by the released extreme point) and is uncorrelated with the overfit indicator ($\rho{=}0.040$) or the PURE--REC gap ($\rho{=}{-}0.082$; SI~Sup.~D; full robustness breakdown in \texttt{results/robustness\_diagnostics.json}). The released evaluator's readout-with-generalization combination is not explained by any within-family overfit--readout relationship.

\paragraph{Leakage sanity checks for the train-pool readout.}
\label{sec:leakage_checks}
The 78.8/70.7\% readout is not a trivial implementation artifact: five controls (SI~Sup.~M) rule out four shortcut classes --- teacher forcing, output--reference misalignment (label permutation collapses BLEU from 78.6 to $\sim$1.0), pose-ignorant ID/cache lookup (input-side pose permutation collapses BLEU to $<$1; a same-signer held-out control reaches only 12.19 BLEU / 3.2\% EM), and narrow-text memorization. The checks do not identify the training mechanism but are consistent with pose-conditioned training-pool exposure.

\paragraph{Distillation and competence extrapolation.}
Fifteen distillation students ($\alpha\in\{0,0.25,0.5,0.75,1.0\}$, three seeds each) all have negative gaps and do not transfer the readout pattern (SI~Table~S26). Competence extrapolation (OLS/GP), per-item gap decomposition, and Gaussian-weight-noise perturbation are reported in SI~Sup.~K and F; neither resolves the identification problem inside the unobserved $(9.8, 13.38)$ interval. The reversal is locally robust near the released checkpoint (Gaussian noise $\sigma\leq3\times10^{-3}$ preserves it within $\pm0.75$; 9 released-weight fine-tunes all pass the competence gate), but these probes test the released checkpoint's vicinity, not an independent construction.

\paragraph{Synthesis.}
Across the three descriptive comparisons, the released evaluator is the only checkpoint combining high training-pool readout (78.8 BLEU, 70.7\% EM) with retained in-domain performance (13.38/12.78). No reconstruction or distilled student reproduces either the $+10.24$ gap or this combination.



\subsection{RQ2: Multi-checkpoint reproduction of the stress-test ordering}
\label{sec:what-drives-reversal}
TN-PURE-v1 yields an original-evaluator gap of $+10.24$ BLEU points (95\% CI $[+8.87,+11.64]$) and TN-PTCOMP-v1 $+6.09$ $[+4.76,+7.41]$; across all fourteen reconstructions both gaps are negative (PURE range $[-1.85,-0.80]$; seed-level 95\% prediction interval $[-1.94,-0.46]$, mean $-1.20$, SD $0.33$, 0/14 positive; the six prospectively specified legacy seeds alone give $[-2.48,-0.28]$ and the eight extension seeds $[-1.61,-0.54]$, with the interval below zero in all fourteen leave-one-seed-out folds), and for the six prospectively specified legacy seeds, original-minus-run difference-in-differences estimates range from $+10.89$ to $+11.99$, every interval excluding zero.

\subsubsection{Recipe-conditioned evaluator reconstructions}
\label{sec:multiseed_factorial}
All fourteen reconstructions share frozen manifests, architecture, hyperparameters, and selection rule; seeds 101--1405 vary initialization and data order (Table~\ref{tab:multiseed_factorial}), and all evaluators are decoded with the canonical \S\ref{sec:retrieval} donor registry (exact-normalized-text exclusion applied).

\begin{table}[h]
\centering
\scriptsize
\caption{Canonical fixed-output evaluation on the released 641-pose PHX-public subset (released evaluator and six prospectively specified legacy seeds (101--606); eight extension seeds in SI~Table~S27). PURE is TN-PURE-v1; COMP is TN-PTCOMP-v1. Gaps are relative to local REC.}
\label{tab:multiseed_factorial}
\begin{tabular}{lrrrrr}
\toprule
Evaluator & REC & PURE & Gap & COMP & Gap \\
\midrule
Released & 12.78 & 23.02 & $\mathbf{+10.24}$ & 18.86 & $+6.09$ \\
Seed 101 & 9.24 & 8.45 & $-0.80$ & 7.92 & $-0.65$ \\
Seed 202 & 8.22 & 6.92 & $-1.30$ & 7.54 & $-1.28$ \\
Seed 303 & 9.10 & 7.25 & $-1.85$ & 8.71 & $-0.96$ \\
Seed 404 & 9.52 & 7.71 & $-1.81$ & 6.38 & $-0.81$ \\
Seed 505 & 9.71 & 8.44 & $-1.28$ & 7.97 & $-1.08$ \\
Seed 606 & 7.55 & 6.31 & $-1.24$ & 7.93 & $-0.16$ \\
\midrule
Extension 707--1405 (range) & \multicolumn{2}{c}{see SI~Table~S27} & $-1.48$ to $-0.87$ & & $-1.03$ to $-0.44$ \\
\bottomrule
\end{tabular}
\end{table}

Competence is assessed on the released 515-sequence development subset, never on PHX-public. The released evaluator obtains dev BLEU-4 13.38; under the uniform full-pool beam-3 free-decode protocol the 14 paper-derived reconstructions obtain dev BLEU-4 6.5--9.4, and the 14 near-faithful runs obtain 7.6--9.8. Their selected validation NLL (2.641--2.712) is lower than the released evaluator's teacher-forced dev NLL (3.235), so likelihood and decoded recognition competence are not interchangeable. Consequently, competence and unavailable original-training details remain confounded with evaluator identity; this is a descriptive observation, not a causal checkpoint-identification result.

\paragraph{Competence gate and trajectory analysis.}
A checkpoint is competence-matched only if both dev BLEU-4 $\geq 12.38$ and dev WER $\leq 86.37$ (one-sided tolerance around released 13.38 / 83.37). None of the 52 gate-eligible non-distillation training runs passes the gate. The 9 released-weight fine-tunes (SI~Sup.~F) all pass (dev BLEU-4 12.92--13.18), but they perturb the released weights rather than constructing from the recipe. The rescue search's best variant (weight-decay 0, seed 202) yields gap $-1.59$. The gate conclusion is threshold-insensitive across a $\pm[0.5,2.0]$ BLEU $\times$ $\pm[1.5,4.5]$ WER grid. Full details in SI~Sup.~D--E.

\paragraph{What the released training log establishes.}
\label{sec:original_trajectory}
The bundle's \texttt{validations.txt} logs dev BLEU-4 reaching 13.38 at step 1{,}820 (Fig.~\ref{fig:competence}a), while all constructible reconstructions plateau at 8--10 by epoch 50--100; the training dynamic behind this slope is not explained by public artifacts. Input degradation (temporal downsampling) compresses both scores but does not eliminate the reversal (at $k{=}8$: REC 8.75, PURE 11.69, gap $+2.94$; SI~Sup.~F).

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{generated_figures/fig3_competence.pdf}
\caption{Decoded competence and training-pool readout separate the released evaluator from every constructible checkpoint. (a) Dev-BLEU-4 training trajectories on a common optimizer-step axis: the released evaluator (orange) reaches 13.38 at step 1{,}820 of 2{,}828 (archived validation log, 202 logged points); fourteen reconstructions form a median band (blue), plus uniformly decoded epoch-25/50 points. (b) Canonical PURE$-$REC gap vs.\ decoded dev BLEU-4 by family: reconstructions (circles), validation-freq-misread/step-corrected/confirmation/long-schedule (squares), rescue (triangle), distillation students (diamonds), released evaluator (star); the gray band marks the observed 6.5--9.8 range (uniform protocol) and the (9.8, 13.38) interval is unobserved by recipe-constructed checkpoints (the nine released-weight fine-tunes occupy 12.92--13.18 but are weight perturbations); degenerate checkpoints are hollow gray points. (c) Full-training-pool free-decode readout (beam-3, full 7{,}060 poses) vs.\ decoded dev BLEU-4: every constructible checkpoint reads out 6.6--9.3 BLEU, while the released evaluator reads out 78.8 BLEU (70.7\% exact match) at dev 13.38. Families are descriptive; checkpoints are not IID replicates.}
\label{fig:competence}
\end{figure}

\paragraph{Cross-metric consistency (SI~Table~S22).}
Under the original evaluator, the PURE--REC gap is positive and large for all decoded-text metrics (BLEU-1 $+20.94$, chrF $+12.93$, ROUGE-L $+17.87$, WER $+13.90$ sign-flipped so positive means PURE has lower error than REC; sign conventions in SI~Table~S22 footnote), while teacher-forced NLL is more negative for PURE ($-0.419$, also sign-flipped), consistent with the likelihood-vs-decoded-metric divergence \cite{jiang2025meaningful}. Under all six prospectively specified legacy reconstructions the gap is negative across every metric. The probe response is therefore a property of decoded-text metrics under the original evaluator, not specific to sacreBLEU-4. Full table in SI~Table~S22.

\paragraph{Rank stability (SI~Sup.~H).}
On a fixed nine-system stress-test set, the released evaluator ranks TN-PURE first; five of six reconstructions rank REC first, with moderate cross-checkpoint agreement (mean $\tau_b{=}0.44$, $\rho{=}0.57$).


\paragraph{Reference-frame sensitivity: matched-subset analysis.}
\label{sec:human_refs}
\label{sec:floor_effect}
Two 2026 corpus audits bear directly on this reversal. Czehmann et al.~\cite{czehmann2026sacred} released manual sign-to-text back-translations of the test set (one deaf fluent L2 DGS signer; per-item confidence flags), documenting information loss, lexical errors, and translationese in the original speech-transcribed references, and showed that reference replacement already changes BLEU scores and rankings on PHOENIX-2014T. Alkain et al.~\cite{alkain2026critical} quantified train--test text overlap across sign-language translation corpora, showing that a small fraction of highly training-like test items disproportionately inflates corpus BLEU. We reproduce the overlap pattern locally: the 32 test items whose reference exactly matches a training text score REC 40.26 / PURE 78.73 (gap $+38.5$), while the remaining 609 items score REC 11.93 / PURE 22.20 (gap $+10.3$).

Czehmann et al.\ compared original- and human-reference conditions on the same confidence=1 subset (462 of 642 items); we follow the same protocol. On the matched 461-item subset (after join with our 641 released test IDs), the original-reference gap is $+10.03$ BLEU points (REC 14.94 / PURE 24.98) and the human-reference gap is $+0.95$ (REC 7.35 / PURE 8.30): a paired attenuation of $9.08$ BLEU points (90.5\%, bootstrap CI $[7.44, 10.86]$ on the absolute attenuation and $[0.81, 1.01]$ on the attenuation ratio; SI~Table~S31; SI~Fig.~S5; per-item paired view in SI~Fig.~S2). The interval for the residual gap includes zero (paired item CI $[-0.13, 1.99]$). This 90.5\% is a \emph{single-annotator, 461-item, post-hoc point estimate}; it cannot isolate any single mechanism because reference replacement simultaneously changes (i) reference content, (ii) expression style/translationese, (iii) BLEU floor level, and (iv) the lexical coupling between retrieval selection and scoring reference. Teacher-forced BT likelihood does not reproduce the probe response under either reference (REC NLL $3.060$ vs PURE $3.479$ on original refs; $3.41$ vs $3.71$ on human refs) \cite{jiang2025meaningful}.

Scoring against both references jointly leaves the gap at $+11.50$, \emph{larger} than original: adding the original reference injects a second lexically coupled target. The probe response is thus specific to reference frames containing the original speech-transcribed register. This is a descriptive contrast, not a causal attribution. Under all six reconstruction checkpoints the human-reference gap is negative ($-0.15$ to $-0.65$), and the asymmetry is consistent across metrics (chrF $82.2\%$, BLEU-1 $84.0\%$; SI~Table~S23). The asymmetric PURE-vs-REC drop (under reference replacement REC drops 50.8\%, PURE drops 66.8\%, a $1.31\times$ relative-drop ratio; the absolute-drop ratio is $16.68/7.59=2.20$) is numerically inconsistent with simple symmetric floor compression (SI~Table~S23), though BLEU's non-linearity precludes a definitive test. The human back-translations are sign-conditioned paraphrases of the recorded test poses, not judgments of the replay donors, so semantic adequacy of TN-PURE-v1 is left unassessed.

\subsection{Donor-origin contrast}
\label{sec:exposure_results}

If the released-evaluator probe response requires exposure to training-pool donors, retrieval restricted to unseen poses should remove or invert it: under the original evaluator, UNSEEN-PURE-v1 (retrieving from the 640 other test poses) scores 8.51 sacreBLEU vs.\ REC 12.78 (gap $-4.27$), a donor-pool substitution estimand of $+14.51$ BLEU points (SI~Table~S30). This is a \emph{donor-pool substitution effect}, not a pure exposure effect: SEEN and UNSEEN differ simultaneously in pool size (7{,}060 vs 640), median donor--reference Jaccard (0.50 vs 0.37), donor reuse (Gini 0.11 vs 0.29), and training-pool exposure.

\paragraph{Matched donor-pool analysis (reviewer-requested SMD$<$0.1 control).}
To separate the donor-similarity confound from the pool-origin effect, we computed a per-item matched subset restricting to items where the seen-donor and unseen-donor Jaccard similarities differ by at most 0.05. This yields 148 items with SMD$=$0.065 on Jaccard (vs.\ $+$0.554 on the full 641). On this matched subset, the origin effect is $+9.56$ BLEU $[+7.47, +11.76]$---still large with CI excluding zero. We do \emph{not} interpret the change from the unmatched $+14.51$ as a percentage attribution: the two estimates come from different target populations ($n{=}641$ vs $n{=}148$), and the caliper selection shifts sentence-length, template-density, and difficulty distributions, so corpus-BLEU differences between them are not additive. This is a Jaccard-balanced selected-subset association, not an identified training-exposure effect: the matched analysis controls for donor-reference Jaccard but not for pool size (7{,}060 vs 640) or the diversity of available donors. Full results in \texttt{results/matched\_donor\_pool.json}; additional balance checks in SI~Sup.~E.

\paragraph{Pool-size-controlled resampling.}
\label{sec:resampling}
A stronger design equalizes pool size directly. We repeatedly subsample 640-item donor pools from the 7{,}060-item training pool (matching the 640-item test pool), apply the same source-Jaccard retrieval rule, and score using the released evaluator's cached training-pool decoded hypotheses (no re-decoding). Over 1{,}000 resamples, the origin effect is $+8.38$ BLEU (median $+8.39$, 95\% sampling interval $[+7.44, +9.34]$, fraction positive $1.00$), reduced from the full-pool $+14.51$ but still large with the interval excluding zero. Pool size and the diversity of the available donors thus account for part of the unmatched difference, but a substantial origin effect persists even at equal pool size. This is still an association---training exposure, donor reuse, and donor diversity remain confounded with pool origin---but it removes the pool-size confound that neither the unmatched nor the matched-subset estimate could. Full distribution in \texttt{results/donor\_pool\_resampling.json}.

\paragraph{Balanced factorial and path decomposition.}
\label{sec:decomposition}
\label{sec:factorial_corrected}
The 605-query factorial (matching seen/unseen donors at high/low LaBSE similarity; SMD $+0.4$ within strata) estimates the pool-origin main effect at $+2.08$ $[+1.56,+2.57]$, similarity at $+6.96$ $[+5.93,+8.08]$, and interaction at $+3.70$ $[+2.63,+4.79]$ (Holm $p{=}0.0003$); across reconstructions the seen main effect shrinks to $+0.51$. Because seen donors remain more similar than unseen within strata, this is a provenance contrast, not an identified training-exposure effect. The matched-subset analysis above provides a tighter estimate; the factorial is retained for its $2\times2$ decomposition.



\subsection{Competitive-system context}
\label{sec:competition_real}
Because the audit's adversarial probe is constructed rather than competitive, the public SLRTP2025 leaderboard offers external context: the ``Ground Truth'' row reproduces our canonical REC numbers exactly (BLEU-4 12.78, \cite{slrtp2025}; SI~Table~S25). The retrieval-based winner USTC-MoE sits roughly even with REC on lexical BT metrics (BLEU-4 $-0.72$; SI~Table~S25). We re-decoded two realistic retrieval outputs under all available evaluators (SI~Sup.~P): the deployment-realistic noisy-gloss retrieval pipeline decodes at 12.65 under the released evaluator (gap $-0.13$ vs.\ REC), and our rebuild of the winner's fixed outputs from its released intermediates decodes at 2.42 (gap $-9.94$; a failed reproduction, not representative of the original entry). Under all 44 non-degenerate reconstructed evaluators, both systems give negative gaps (noisy-gloss range $[-1.94,-0.07]$; USTC-MoE range $[-8.30,-4.28]$). Neither realistic system recovers a retrieval-favoring ordering under any evaluator. The $+10.24$ gap is specific to the constructed adversarial probe, and we do not claim that real competition rankings would change across checkpoints. Archiving fixed per-team outputs alongside public scores is the most direct suggestion we can offer for enabling rank-stability audits.

\paragraph{Per-item gap decomposition (experiment C, SI~Sup.~K).}
An OLS regression ($R^2=0.33$, $n=641$) attributes the largest share of per-item gap variance to high-overlap-train count, template density, and donor--reference Levenshtein distance; removing the signer block changes $R^2$ by $\leq 1\%$ ($\Delta R^2$). Full model, coefficient table, and figure in SI~Sup.~K.

\section{Discussion}
The audit yields three findings organized around the question of whether the publicly documented recipe is sufficient to reproduce the released SLRTP2025 BT evaluator and its behavior on a constructed replay probe.

\emph{RQ1---Failed public-artifact reconstruction.} Using our disclosed reconstruction implementation and the publicly released artifacts, none of the 14 near-faithful runs (4 validation-freq-misread, 10 step-corrected) or 14 legacy-implementation seeds reached the released evaluator's competence (dev BLEU-4 6.5--9.8 vs.\ 13.38), training-pool readout (7.2--9.3 vs.\ 78.8), or teacher-forced token accuracy (55--60\% vs.\ 96.5\%; Table~\ref{tab:sanity_stats}). Three descriptive comparisons (Table~\ref{tab:diagnostic_summary}) all place the released evaluator far outside any constructible range. The source of the gap remains unidentified.

\emph{RQ2---Descriptive non-reproduction under competence-unmatched reconstructions.} Because every constructible checkpoint is less competent than the released evaluator (dev BLEU-4 6.5--9.8 vs.\ 13.38), the non-reproduction of the adversarial probe response (43 of 44 non-degenerate gaps negative, range $[-2.01,+0.24]$; one step-corrected seed gives a small positive gap of $+0.24$) \emph{cannot distinguish} whether it reflects the competence gap, an unrecovered training mechanism, or their interaction. This is a descriptive observation about our public-artifact reconstruction attempt, not a causal checkpoint-identity claim. Without a competence-matched independent implementation---which we could not obtain (the released repo is eval-only; our trained checkpoints were lost in a host compromise)---the identification problem is unresolved. The descriptive step-corrected 10-seed prediction interval $[-1.89,+0.45]$ includes zero, whereas the prospectively specified 6-seed legacy interval $[-2.48,-0.28]$ does not; the confirmatory non-reproduction direction rests on the latter. Cross-evaluator re-decoding of a realistic noisy-gloss retrieval output yields a released-evaluator gap of $-0.13$ and 43/44 negative gaps, confirming the probe response is specific to the constructed adversarial design.

Because all constructible checkpoints are less competent than the released evaluator (decoded family max dev BLEU-4 9.8, training-log max 10.02, vs.\ 13.38), this non-reproduction is scoped to the public recipe and does not constitute a causal checkpoint-identity claim. Two local-robustness checks on the released checkpoint (Gaussian weight noise and fine-tuning from released weights, SI~Sup.~F) confirm the reversal is locally robust but probe only the vicinity of the released weights. Competence-extrapolation and per-item decomposition analyses (SI~Sup.~K) are exploratory and do not resolve the identification problem. Our rebuilt USTC-MoE output (SI~Table~S33) is pipeline-faithful but not score-faithful (2.42 vs.\ official 12.06) and should not be taken as representative of the original competition entry. The noisy-gloss result provides the primary realistic-system evidence.

\emph{RQ3---Reference-frame sensitivity and donor-origin coupling.} On the matched 461-item confidence=1 subset of Czehmann et al.'s~\cite{czehmann2026sacred} human back-translations, reference replacement is associated with a gap reduction to $+0.95$ from $+10.03$---a single-annotator, 461-item, post-hoc point estimate of 90.5\% attenuation. This estimate cannot isolate any single mechanism because reference replacement simultaneously changes (i) reference content (speech transcription vs.\ sign-conditioned back-translation), (ii) expression style and translationese, (iii) BLEU floor level (REC drops 7.59, PURE drops 16.68), and (iv) the lexical coupling between retrieval selection and the scoring reference. The asymmetric PURE-vs-REC drop (relative-drop ratio $1.31\times$: REC drops 50.8\%, PURE drops 66.8\%; absolute-drop ratio $2.20\times$) is numerically inconsistent with simple symmetric floor compression, but BLEU's non-linearity and the coupling structure preclude a definitive floor-effect test. The matched donor-pool analysis (\S\ref{sec:exposure_results}) shows the origin effect persists at $+9.56$ under SMD$<$0.1 Jaccard matching (148 items); the pool-size-controlled resampling (640-vs-640, \S\ref{sec:resampling}) gives $+8.38$ $[+7.44, +9.34]$. We report these as Jaccard-balanced and pool-size-controlled associations, not percentage attributions. The donor-pool substitution reverses the gap sign (UNSEEN-PURE-v1 scores 8.51 vs.\ REC 12.78, gap $-4.27$); all three origin-effect estimates remain associations, not identified training-exposure effects, because donor diversity, reuse, and training exposure are not independently manipulable.

\paragraph{Relation to prior work.}
These findings extend prior critiques of BT validity from output faithfulness to public-recipe reproducibility: improved BT scores need not imply target conditioning \cite{hong2026conditional}, low recorded-pose BT baselines are documented \cite{walsh2024datadriven}, and BT likelihood is a more stable correlate of human judgment than decoded BLEU \cite{jiang2025meaningful}. Our reversal is a \emph{decoded-sacreBLEU} reversal --- teacher-forced BT likelihood does not reproduce it (REC NLL $3.060$ vs PURE $3.479$) --- and whole-donor replay is a constructed probe, whereas SOKE-style word-level retrieval can improve pose quality under a different representation \cite{soke}.

\paragraph{Value of the audit infrastructure despite the competence confound.}
The competence-matched condition is unmet, so RQ2's non-reproduction cannot be attributed to a causal checkpoint effect. This constrains what the audit \emph{infers} but not what it \emph{provides}: (i) the provenance table (Table~\ref{tab:provenance}) and three-way data reconciliation establish the released materialization differs from the official split by 41 IDs; (ii) the checkpoint registry with SHA-256 deduplication (SI~Sup.~D) prevents double-counting; (iii) the unified-inference regression test (\S\ref{sec:sanity_checks}) isolates code-path defects from recipe insufficiency; (iv) the leakage controls (SI~Sup.~M) are extensible to any learned evaluator; (v) the single canonical materialization with four-layer reproduction (\S\ref{sec:method}) is a concrete model for reproducible benchmarking infrastructure. These contributions are transferable to future learned-evaluator audits.

\paragraph{Actionable recommendations for benchmark organizers.}
The audit yields three transferable lessons, scoped to the constructed replay-probe stress case on PHX-public rather than to general SLP leaderboard stability.

\emph{Lesson 1: a released evaluator is a research artifact, not an infrastructure component.} The released \texttt{config.yaml} alone was insufficient for 78 reconstruction runs across 13 families to reach competence parity, and the readout pattern could not be localized to a training stage because intermediate checkpoints are unpublished. Benchmarks should release (i) the exact training command and seeds, (ii) data pipeline scripts beyond the manifest, (iii) a SHA-256 manifest of training tensors, (iv) intermediate checkpoints at least every 100 epochs, and (v) the fixed per-team pose outputs alongside the public scores; the current release provides only the Progressive Transformer baseline output, so rank-stability audits across independently trained evaluators cannot be reproduced by third parties on real competition systems.

\emph{Lesson 2: on template-dense benchmarks, retrieval entries may benefit from donor-exclusion disclosure.} A donor-exclusion sweep is cheap and exposes the retrieval advantage on PHX-public's template-dense register; retrieval-based leaderboard entries \emph{may} voluntarily attach $\tau \in \{0.3, 0.5\}$ donor-exclusion results as a transparency practice on template-dense benchmarks. The training-pool free-decode BLEU ratio (released: $\text{78.8}\,/\,\text{13.38}\approx5.9$; reconstructions: 0.93--1.12) and input-side pose permutation with output-equivariance verification (SI~Sup.~M) are two descriptive probes we found informative in this case study; whether they generalize to other benchmarks is untested.

\emph{Lesson 3: reference-coupling artifacts warrant a supplementary reference check.} On PHX-public, the 90.5\% attenuation under Czehmann et al.~\cite{czehmann2026sacred} back-translations is a single-annotator reference-frame case study showing that the gap is sensitive to the reference frame; it should not be generalized beyond this signer's interpretation. Benchmarks on similarly template-dense corpora could adopt human back-translations as a supplementary reference set, follow the matched-subset protocol, and flag systems whose gap shifts between the two; the cost is one reference file and a paired bootstrap. Whether this generalizes beyond PHX-public's lexical-coupling structure is untested.

\paragraph{Limitations.}
The audit is confined to PHX-public, a template-dense weather domain with documented reference-validity and train--test-overlap issues~\cite{czehmann2026sacred,alkain2026critical}. Per-team pose outputs are not archived; our cross-evaluator studies decode at or below REC under all 44 non-degenerate evaluators. All constructible checkpoints decoded on PHX-public (44 non-degenerate; Table~\ref{tab:accounting}) are less competent than the released evaluator (family decoded max dev BLEU-4 9.8, training-log max 10.02, vs.\ 13.38). The remaining training runs either were not PHX-public-decoded (large-arch, lr-rescue, holdout), produced degenerate output ($\alpha{=}1.0$ distillation students, smallest ladder fractions), or were evaluated on holdout splits only. Without a competence-matched independent implementation, the causal checkpoint-identity question is unresolved. The human back-translations are a single-annotator resource without second-annotator adjudication; the 90.5\% attenuation is conditional on one signer's interpretation and should not be taken as a population parameter (qualitative direction is stable across confidence tiers; SI~Table~S31). The probe response is a decoded-BT-text-metric effect, not a sign-language quality judgment; without target-conditioned DGS signer evaluation, semantic adequacy of the donor motion is unassessed.

\section{Conclusion}
This audit reports a failed public-artifact reconstruction attempt of the released SLRTP2025 BT evaluator and a descriptive non-reproduction of a constructed adversarial probe response.

For RQ1, none of our 14 near-faithful or 14 legacy-implementation reconstructions reached the released evaluator's competence; the source of the gap remains unidentified. For RQ2, 43 of 44 non-degenerate constructible checkpoints give negative probe gaps (one step-corrected seed gives $+0.24$), but because every constructible checkpoint is less competent than the released evaluator (dev BLEU-4 6.5--9.8 vs.\ 13.38), we cannot distinguish whether this reflects the competence gap, an unrecovered training mechanism, or their interaction. For RQ3, reference replacement is associated with a gap reduction to $+0.95$ (interval includes zero) in a single-annotator, 461-item case study that cannot isolate reference-text defects from lexical-coupling disruption, paraphrase shift, or BLEU floor effects.

The scope is bounded: no causal checkpoint effect, no identified memorization mechanism, and no general leaderboard-rank instability is established. We frame the replay probe as an adversarial lexical-coupling stress test throughout. The audit's lasting contributions are the provenance, checkpoint-registry, and evaluator-audit infrastructure, transferable to future learned-evaluator audits.

\section*{Statements and Declarations}
\label{sec:ethics}
\paragraph{Funding.} The authors declare that no funds, grants, or other support were received.
\paragraph{Conflict of interest.} The authors declare no competing interests.
\paragraph{Data governance and ethics.} Although all datasets are publicly released, pose tensors retain signer identity, biometric features, and signing style. We address four governance dimensions. (i) \emph{Consent scope:} we conduct this research under the published licences of PHOENIX-2014T and SLRTP2025; however, the original consent forms are not publicly available, so we cannot independently confirm that the informants' consent covers derivative replay-probe analysis. No new human-subjects data was collected. (ii) \emph{Licensing:} PHOENIX-2014T is distributed under its own academic research licence (the version specified in the distributed copy; the official landing page does not display a Creative Commons version); the Czehmann et al.~\cite{czehmann2026sacred} back-translations are CC BY-NC-SA 4.0; CSL-Daily and SLRTP2025 carry academic-use licences; the licence compatibility matrix is documented in the artifact. (iii) \emph{Identity re-identification risk:} we release per-item decoded text (low re-identification risk) but do \emph{not} release per-signer raw poses; the artifact ships only aggregated metrics, decoded hypotheses, and analysis scripts. (iv) \emph{Deaf community involvement:} no Deaf/DGS stakeholders were involved in formulating the research questions or interpreting the results of this audit; we identify this as a limitation. The absence of Deaf co-investigators limits the ecological validity of our interpretations: the adversarial-probe framing, the reference-frame analysis, and the benchmark recommendations reflect a hearing-researcher perspective on a DGS resource, and Deaf-led sign-language AI research norms should guide any follow-up~\cite{desai2024systemic}. The Czehmann et al.\ back-translations we build upon were produced by a Deaf fluent L2 DGS signer.
\paragraph{Data availability.} All numerical values, per-checkpoint data, decoded hypotheses, and analysis scripts are in the artifact at \url{https://anonymous.4open.science/r/rcp-audit-artifact-B314/README.md}. The artifact is version-controlled (git commit \texttt{87f7fff}, 2026-08-09); a claim-to-file manifest mapping each headline number to its source file and verification command is at \texttt{artifact/claim\_manifest.json}, and a single-command consistency check (\texttt{python3 scripts/check\_paper\_consistency.py}) verifies the paper's counts against the registry. A persistent DOI-bearing Zenodo release will be deposited at submission, not deferred to acceptance. The supplementary material (\texttt{supplementary.pdf}) contains all SI appendices. Upstream datasets are used under their published licences (PHOENIX-2014T under its academic licence; the Czehmann et al.\ human back-translations under CC BY-NC-SA 4.0; CSL-Daily and SLRTP2025 under their academic licences).
\paragraph{Author contribution.} Both authors contributed to the study conception, design, data analysis, and manuscript preparation. Both authors read and approved the final manuscript.
\appendix
\section*{Supplementary Information (SI)}
\addcontentsline{toc}{section}{Supplementary Information}
The following supplementary materials, referenced throughout the main text, are available as a separate file (\texttt{supplementary.pdf}) and in the anonymous artifact. Each appendix is annotated with the question it answers.
\begin{itemize}\itemsep0pt\footnotesize
\item Sup.~A: Missing Test ID Sensitivity --- does the absent test item change the headline gap? (\texttt{sec:appendix\_missing\_id})
\item Sup.~B: Per-seed Extension Cells 707--1405 --- does the prediction interval hold beyond the six prospectively specified legacy seeds?
\item Sup.~C: Analysis Provenance --- evidentiary role and multiplicity handling of each analysis. (\texttt{sec:provenance})
\item Sup.~D: Canonical Checkpoint Registry --- what was trained, and which families enter which claim? (\texttt{tab:checkpoint\_registry})
\item Sup.~E: Donor-Pool Substitution Decomposition --- how much of the seen-pool advantage is pool-origin vs.\ similarity? (\texttt{sec:appendix\_decomposition}, \texttt{tab:path\_sensitivity})
\item Sup.~F: Config-Faithful and Perturbation Details --- protocol-mismatch check, weight-noise sweep, epoch-checkpoint decoupling search. (\texttt{sec:appendix\_config\_faithful}, \texttt{sec:appendix\_perturbation}, \texttt{tab:perturbation})
\item Sup.~G: Cluster-Aware Bootstrap --- does the gap survive resampling by signer, broadcast-show, and date? (\texttt{sec:appendix\_cluster\_bootstrap})
\item Sup.~H: Supplementary Oracle Diagnostics --- is the evaluator-aware advantage robust to reference-overlap exclusion? (\texttt{sec:appendix\_oracle})
\item Sup.~I: Template-Density, Holdout Boundary, Donor-Exclusion --- does the reversal persist on a clean template-holdout split and under donor-text exclusion? (\texttt{sec:appendix\_template\_boundary})
\item Sup.~J: Transcript-Slot Diagnostic Details --- do slot-level scores reproduce the BLEU reversal? (\texttt{sec:appendix\_slot})
\item Sup.~K: Competence Extrapolation and Per-Item Gap Decomposition --- could competence in the unobserved interval explain the reversal, and which item features drive the gaps? (\texttt{sec:appendix\_extrapolation})
\item Sup.~L: CSL-Daily Boundary Control --- does the readout pattern generalize beyond PHX-public? (\texttt{sec:csl-daily})
\item Sup.~M: Leakage Sanity Checks --- can the train-pool readout be explained by implementation shortcuts? (\texttt{sec:appendix\_leakage})
\item Sup.~N: Pre-Exclusion Materialization Sensitivity --- how sensitive are the headline numbers to the donor-exclusion rule? (\texttt{sec:appendix\_materialization})
\item Sup.~O: BLEURT-Substitute Validation --- does a learned metric corroborate the BLEU finding under reference replacement? (\texttt{sec:appendix\_bleurt})
\item Sup.~P: Main-Text Tables (SI~Table~S22--S33) --- decoded-metric sensitivity, floor effect, cross-metric gaps, leaderboard, distillation ladder, checkpoint registry, protocol summary, rescue sweep, exposure estimands, reference sensitivity, and cross-evaluator re-decoding of noisy-gloss and USTC-MoE outputs. (\texttt{sec:appendix\_moved\_tables})
\item Sup.~Q: Additional Protocol Details --- tie-breaking, scorer equivalence, metric implementations, audit-design, reference-frame, and donor-origin figures (SI~Fig.~S4--S6). (\texttt{sec:appendix\_moved\_protocol})
\item Sup.~R: Reconstruction Sanity Checks (SI~Table~S35) --- small-sample overfit test, unified inference regression, and per-checkpoint teacher-forced vs.\ free-decode diagnostics ruling out a systematic code-path defect. (\texttt{sec:appendix\_sanity})
\end{itemize}

Full checkpoint registry in SI~Sup.~D.

\bibliography{references}

\end{document}
