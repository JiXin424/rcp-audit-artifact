\documentclass[pdflatex,sn-chicago]{sn-jnl}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{url}
\usepackage{xcolor} % required by \email (\authemail uses \textcolor)

\title[Reconstructing the SLRTP2025 Back-Translation Evaluator]{Reconstructing the SLRTP2025 Back-Translation Evaluator: An Artifact Audit and Gradient-Clipping Sensitivity Study}

% LRE single-blind review: author information IS shown.
\author*[1]{\fnm{Xin} \sur{Ji}}
\author[1]{\fnm{Cheng} \sur{Zhang}}
\email{2024540101@bipt.edu.cn}
\affil*[1]{\orgname{Beijing Institute of Petrochemical Technology}, \orgaddress{\city{Beijing}, \country{China}}}

\begin{document}

\abstract{We audit whether the released SLRTP2025 back-translation (BT) evaluator can be reconstructed from its public artifacts, an evaluation-only repository without training scripts. A line-level audit of our own reconstruction code identified seven defects relative to the released configuration's semantics; the corrected re-implementation recovers most of the evaluator's in-domain competence (eight seeds: dev BLEU-4 11.59--12.62, mean test-split decoding 12.27 $\pm$ 0.46, versus the released 13.38 / 12.78).

Two qualitative signatures nevertheless fail across all 69 non-degenerate runs, which share one undisclosed author inference (gradient-clip threshold 1.0): the training-pool memorisation signature (released 78.8 BLEU / 70.7\% exact match; panel ceiling 12.08 / 2.5\%) and a positive response to a constructed replay probe (released $+10.24$, post-selected). A clipping sensitivity grid resolves both: with clipping disabled---the candidate upstream framework's default when, as in the released configuration, no clip field is specified---the reconstruction reproduces both signatures (readout 99.98 / 99.9\%; probe gap $+11.19$), confirmed across three seeds on a never-explored dev split ($+8.0$ to $+8.6$; released $+8.10$). Readout and gap vary monotonically across the clip ladder; the released operating point's coordinates are bracketed by different thresholds but not matched jointly. We claim qualitative signature reproduction---not numerical reconstruction---with the clipping attribution resting on framework source semantics plus behavioural consistency, not a released training transcript.

The probe response was observed only under the tested reference-coupled retrieval conditions: it falls to $+0.95$ under human back-translations and is absent for deployment-realistic systems. The transferable lesson is that an undocumented framework default can make reproducible properties look unreproducible.}

\keywords{sign language production, back-translation evaluation, reproducibility audit, reference-frame sensitivity, learned evaluation metrics, PHOENIX-2014T}

\maketitle

\section{Introduction}

Sign language production (SLP) systems are commonly evaluated by back-translation (BT): a trained sign recognizer decodes each output pose sequence, and a text metric compares the decoding with the reference. This measures recognizer-readable content through a learned measurement model, so system rankings may reflect both the evaluated outputs and the inductive biases of a particular evaluator checkpoint. The present audit is framed as a \emph{public-artifact reconstruction attempt}: whether our implementation, using the released SLRTP2025 public artifacts, can reconstruct the released BT evaluator. We address two research questions:

\begin{enumerate}
\item[RQ1.] Can a best-effort, artifact-conditioned implementation of the released training configuration---with every disclosed defect corrected and every remaining inference bracketed by sensitivity runs---reconstruct the released evaluator's competence and training-pool readout?
\item[RQ2.] How sensitive is the constructed probe's response to the reference text and to donor origin?
\end{enumerate}

\noindent Beyond the two research questions, we report a \emph{descriptive observation} (not a research question): the two signatures that initially failed to reproduce---the training-pool memorisation signature and the probe response---are blocked not by an unrecovered training mechanism but by a single author inference in our own reconstruction. Across the 69 non-degenerate constructible runs, all of which share a gradient-clip threshold of 1.0, the readout stays at or below 12.08 BLEU (2.5\% exact match) versus the released evaluator's 78.8 / 70.7\% and every probe gap is negative; yet these runs already match the released evaluator's test-split decoding competence (family mean 12.27 vs.\ 12.78), so the shortfall is not surface competence. The sensitivity grid of \S\ref{sec:sensitivity} then shows both signatures to be a \emph{clipping-regime} effect: with clipping disabled---the default of the candidate upstream framework when the released configuration specifies no clip fields, as it does---the reconstruction reproduces both signatures, with intermediate thresholds interpolating monotonically between the regimes (\S\ref{sec:sensitivity}). Throughout we distinguish qualitative signature reproduction (demonstrated) from numerical reconstruction of the released operating point (not achieved) and mechanism confirmation that the released training itself ran unclipped (not established; \S\ref{sec:three_levels}). The replay probe itself is an \emph{adversarial lexical-coupling stress test}: the source-conditioned retriever selects donors by surface vocabulary overlap with the German source, which is identical to the scoring reference in PHX-public, so the probe deliberately targets the strongest form of the shortcut rather than a deployable retrieval pipeline.

Prior work shows BT scores can be modest for recorded signing and can improve without faithful target conditioning \cite{walsh2024datadriven,hong2026conditional} (the latter a preliminary preprint); sign-native or pose-aware metrics provide complementary evidence \cite{signbleu,silverscore,jiang2025meaningful}, yet BT remains the standardized infrastructure on which the SLRTP2025 ranking depends \cite{slrtp2025}. Chen et al.~\cite{chen2022repro} showed that learned metrics can fail to reproduce due to undocumented preprocessing and missing code---an anatomy directly relevant here, where seven defects in our own reconstruction were identified and corrected. What prior audits of PHOENIX-2014T have not shown is whether the released evaluator itself is reconstructible: under it, source-conditioned whole-donor retrieval scores 23.02 sacreBLEU versus 12.78 for the recorded test poses, and the question is whether that response and the evaluator's training-pool behaviour can be rebuilt from the public release.

\paragraph{Why a constructed probe matters.}
Retrieval-augmented SLP entries draw donors from the evaluator's training pool, so the adversarial probe isolates a risk that production systems face in diluted form. The audit contributes a transferable template (provenance table, checkpoint registry with SHA-256 deduplication, evaluator-replication controls, teacher-forced-vs-free-decode diagnostics) for public-artifact reproducibility testing. The specific probe response is a PHX-public released-evaluator case study and does not support general claims about BT evaluation or retrieval-based SLP.

\section{Related Work}

\noindent\textit{Note on citations and preprints.} Two 2026 PHOENIX-2014T audits we build on are peer-reviewed: Czehmann et al.~\cite{czehmann2026sacred} (LREC~2026 12th Workshop on the Representation and Processing of Sign Languages) and Alkain et al.~\cite{alkain2026critical} (Frontiers in Artificial Intelligence). Where we cite 2025--2026 arXiv preprints \cite{hong2026conditional,backtranslation2,artiaga2025signer,desai2024systemic}, we treat their findings as preliminary reports rather than peer-reviewed results; conclusions in this audit do not hinge on the correctness of any single preprint. All arXiv citations were accessed on 2026-08-10.

\paragraph{Learned evaluation and its reproducibility.}
Neural SLP often evaluates with a frozen sign recognizer \cite{camgoz2018neural,saunders2020progressive}; Saunders et al.~\cite{saunders2021continuous} proposed BT as an SLP evaluation protocol, arguing it shifts absolute scores but not rankings between similar configurations---a condition unmet in our family. SignBLEU \cite{signbleu}, SiLVERScore \cite{silverscore}, and human-correlated metrics \cite{jiang2025meaningful,backtranslation2} offer complementary views, but BT BLEU remains the standardized infrastructure on which the SLRTP2025 ranking depends \cite{slrtp2025}; auditing its reproducibility is infrastructure maintenance, not a metric-superiority claim. Chen et al.~\cite{chen2022repro} showed that reproducing BERT-based evaluation metrics can fail through undocumented preprocessing, missing code, and baseline differences even when weights are released; our audit targets a full evaluator pipeline and adds a resolved case---the failure traced to an undisclosed author inference, not to the artifacts. The initial non-reproduction was demonstrated across 69 non-degenerate decoded runs (all with strictly negative gaps; five degenerate excluded; all sharing clip 1.0), corresponding to 68 distinct SHA-256 weight binaries after one byte-identical collision is deduplicated.

\paragraph{Retrieval, shortcuts, and corpus audits.}
Retrieval-based SLP ranges from phonetic composition to retrieval-enhanced generation \cite{walsh2024datadriven,signstitching,soke,darslp}, and SLRTP2025's leading entry is retrieval-based \cite{slrtp2025}; memorization results in retrieval-augmented language modeling show strong task scores can reflect reproduction of training examples \cite{carlini2021extracting,kandpal2022memorization,khandelwal2020knnlm}. Model-based generation metrics are further criticized by \cite{yazdani2025critical,backtranslation2,he2023blind}. Two 2026 PHOENIX-2014T audits bear directly on our reversal: Czehmann et al.~\cite{czehmann2026sacred} released human back-translations and documented reference information loss, Alkain et al.~\cite{alkain2026critical} showed the highest train--test text overlap among tested SLT corpora, and Artiaga et al.~\cite{artiaga2025signer} showed signer-dependent evaluation overestimates SLT capability; none runs multi-checkpoint evaluator experiments or donor-pool substitution controls.

\paragraph{PHOENIX-2014T as a language resource: known limitations.}
PHOENIX-2014T~\cite{camgoz2018neural} (8,257 weather-forecast interpretations by 9 signers) is the most widely used DGS parallel corpus, but four properties constrain generalizability: (i) template density inflates train--test n-gram overlap~\cite{alkain2026critical}; (ii) reference texts are speech transcriptions, not DGS translations~\cite{czehmann2026sacred}; (iii) the 178-keypoint pose materialization encodes joint centres and coarse facial geometry---some finger articulation, palm orientation, and mouth shape---but not appearance texture, hand--body contact, occlusion-robust depth, or reliable eye gaze (our MSKA control uses 133 keypoints; SI~Sup.~L); (iv) signer-dependent evaluation can overestimate capability~\cite{artiaga2025signer}. The lexical coupling between source and reference amplifies these effects in our adversarial probe; the transferable audit components (provenance table, checkpoint registry, leakage controls) apply to any learned evaluator paired with a public training corpus.

\section{Methods}
\label{sec:method}

\subsection{Evaluation design}
Let $E_c$ denote a BT evaluator checkpoint, $x_n$ the source sentence (German) for item $n$, and $o_{i,n}$ the pose sequence for output condition $i$ and item $n$; $E_c$ produces $h_{c,i,n}=E_c(o_{i,n})$ and $B_{c,i}=M(\{(h_{c,i,n},y_n)\}_{n=1}^{N})$, where $y_n$ is the reference text and $M$ is the corpus-level sacreBLEU functional unless stated otherwise. We call a result a \emph{descriptive non-reproduction} when a probe response observed under the released evaluator does not appear across independently constructed checkpoints under a fixed output set; the term is descriptive and does not imply a causal checkpoint-identity effect. The fixed primary set comprises recorded test poses (REC), random donor copy, source-conditioned whole-donor retrieval, a Progressive Transformer generator, and retrieval--generator compositions; oracle-gloss variants are diagnostic controls.

\paragraph{Operational definition of reconstruction competence.}
\label{sec:sufficiency_def}
For table presentation we summarise competence with an \emph{author-operational} gate (dev BLEU-4 $\geq 12.38$ and dev WER $\leq 86.37$, a one-sided $\pm$1 BLEU / $\pm$3 WER tolerance around the released evaluator's 13.38 / 83.37). The gate is defined post-hoc by us, is not a community standard, and other tolerances give other pass counts; its threshold-insensitivity grid, the pass/fail table, and the rationale are relegated to SI~Sup.~D--E, and the primary evidence throughout is the \emph{continuous} metric comparison (seven-correction dev BLEU-4 11.59--12.62 and train readout 11.45--12.08 vs.\ released 13.38 and 78.8).

The main evaluation uses PHX-public, a German weather-domain text-to-pose benchmark whose released SLRTP2025 pose records contain 7,060/515/641 sequences, against the official PHOENIX-2014T split sizes of 7,096/519/642 reported by the SLRTP2025 repository: 36/4/1 official IDs are absent from the released pose materialization, and every result is conditioned on the complete release. An ID-level reconciliation (\texttt{results/split\_reconciliation.json}; SI~Sup.~A) classifies all 41 missing IDs: each is present in an independent upstream skeleton extraction that carries the full official counts, so the drop occurred inside the SLRTP2025 pose packaging rather than upstream; the dropped items show no duration or signer anomaly (train frames 22--262, spanning the corpus length range; 36 train items spread over six of the nine signers, none contributing more than ten), and only two of the 36 dropped training texts lack an exact copy in the retained pool (both high-frequency template sentences, so template density is essentially unaffected). The evaluator bundle's shipped \texttt{train.pt} contains exactly 7,060 items---the released training manifest is itself the reduced pool, so the memorisation signature we audit is defined over the same 7,060 items the released evaluator trained on. The three-way provenance table (official split, pose materialization, evaluator training manifest) is in SI~Sup.~P. Supplementary analyses use \textbf{PHX-holdout-MSKA} (133-joint, 1,538 windows) and CSL-Daily (Mode B, 1,176 sequences; SI~Sup.~L); the protocol summary is in SI~Table~S38.



All pose sequences are evaluated at 12.5\,fps, matching the SLRTP2025 evaluator training distribution. The data flow is: official PHOENIX-2014T video at 25\,fps $\to$ SLRTP2025 skeleton extraction at 25\,fps $\to$ our \texttt{[::2]} subsampling to 12.5\,fps for evaluator input; the official evaluation command uses \texttt{-{}-fps 25} (the input file's native rate), but the evaluator was trained on 12.5\,fps-subsampled poses, so our subsampling matches the training distribution rather than the command-line flag. A three-path regression confirms no double subsampling (details in SI~Sup.~F). PHX-public uses $\alpha{=}0.88$ (selected on dev); the supplementary splits use $\alpha{=}0.5$.

\subsection{Systems and retrieval controls}
\label{sec:retrieval}
For each target, source-conditioned whole-donor retrieval scans the complete training pool after Unicode NFKC normalization, whitespace normalization and lowercasing, selecting the sequence with maximum source-text token-set Jaccard similarity. Oracle-gloss retrieval instead uses target gloss and is treated as an input-leakage diagnostic; random retrieval selects a donor uniformly. Donor exclusion removes exact normalized text or gloss duplicates or donors above threshold $\tau$; gloss-based exclusion is therefore an offline diagnostic, not a deployable inference procedure.

\emph{Single canonical materialization.} Every number in this paper is generated from one canonical donor registry built by the deterministic \S\ref{sec:retrieval} algorithm (exact-normalized-text exclusion applied), whose SHA-256 is \texttt{9170a53026ab3263b451a3632a9e02\-318745acabf12f0b679921477afbefa301}. Reproduction is supported at four layers (\S~Sup.~Q, Table~S44): (i) \textbf{L1---recompute selected reported statistics}: a single command (\texttt{make core-audit}) regenerates the registry and verifies headline numbers from committed per-item decodes; it does \emph{not} retrain models or decode from checkpoints, and does not verify every number in the paper; (ii) \textbf{L2---analysis re-execution}: each analysis script re-executes from the committed per-checkpoint data and hypotheses; (iii) \textbf{L3---model re-evaluation}: re-decoding fixed outputs from checkpoint weights requires the training bundle; (iv) \textbf{L4---from-scratch retraining}: full retraining requires the restricted PHOENIX-2014T licence, the exact environment, and approximately 200 GPU-hours. The \texttt{make core-audit} command executes L1 only and is not an end-to-end reproduction. A claim-to-file manifest mapping each headline number to its source file and verification command is in \texttt{claim\_manifest.json} (artifact root). An earlier pre-exclusion materialization (PURE 23.79, gap $+11.01$) survives only as a sensitivity analysis in SI~Sup.~N.

The per-claim accounting that was tabulated here in earlier drafts (verification layer, required inputs, licensed third-party end-to-end verifiability, compute, and failure condition per headline claim) is in SI~Sup.~Q and Table~S50 (SI~Sup.~V); the L1/L2 commands exit non-zero on any mismatch and were executed on the submitted artifact (exit 0; run transcripts accompany the revision materials).

\paragraph{Lexical coupling of the source-conditioned probe (adversarial design).}
In PHX-public the source sentence is identical to the reference text at the sequence level, and the TN-PURE-v1 retriever selects donors by token-set Jaccard similarity to that same text, so the probe deliberately targets the strongest form of the lexical-coupling shortcut: the $+10.24$ gap is the expected output of an adversarial design, not a discovery about ranking behaviour, and the human-reference analysis of \S\ref{sec:human_refs} therefore mixes reference-text properties, coupling disruption, paraphrase shift, and BLEU floor effects (full framing in SI~Sup.~T).

\paragraph{Retrieval composition and donor-origin probe systems.}
TN-PURE-v1 uses whole-donor copy at 12.5\,fps; TN-PTCOMP-v1 composes a PT scaffold with donor hands and face via $\alpha$-blending ($\alpha{=}0.88$; joint-set definitions in the artifact). Frozen donor-origin probes are \textbf{UNSEEN-PURE-v1} (retrieval from the 640 other test poses), \textbf{CROSSFIT-PURE-A/B} (hash-split folds), and \textbf{SEEN-PURE-MATCHED-v1} (a train-pool donor within $\pm0.1$ Jaccard of each UNSEEN donor; 622/641 admit one); all are constructed probes, not deployable generators (construction rules in the artifact; SI~Sup.~E).

\subsection{Evaluator checkpoint family}
The main inferential checkpoint family comprises the original frozen SLRTP2025 evaluator and 36 \emph{recipe-conditioned} runs trained from scratch with the documented architecture on a common frozen partition: the eight \emph{seven-correction} runs (\S\ref{sec:faithful_v4}; seeds 42--49), fourteen \emph{partially-corrected} runs (4 validation-freq-misread $+$ 10 step-corrected), and fourteen \emph{legacy} runs (6 prospectively specified seeds 101--606 $+$ 8 extension seeds 707--1405). The original bundle's \texttt{validations.txt} logs 202 validation points over 2{,}828 optimizer steps ($\approx$102 epochs), with decoded-BLEU selection reaching best dev BLEU-4 13.38 at step 1{,}820; the trajectory is analyzed in \S\ref{sec:original_trajectory}, and intermediate checkpoints are not published. All recipe-conditioned runs share the documented architecture (3L/256/512ff, 8 heads), tokenizer, manifests (7{,}060/515), $[::2]$ subsampling, Adam (1e-3, wd .001), batch size 256, and plateau $\times0.8$ LR schedule (full provenance in SI). In total 76 from-scratch training runs across 12 families (registry in SI~Sup.~D); 74 were beam-3 decoded under the canonical donor registry (73 unique weight binaries; one byte-identical collision), of which five degenerate runs are excluded---leaving 69 non-degenerate decoded runs (68 unique binaries), all with strictly negative gaps. All 69 share code, data, architecture, and the clip-1.0 inference (\S\ref{sec:sensitivity}); they are coverage evidence, not independent replications. The auto-generated accounting table is in SI~Sup.~P.

\paragraph{What the 76 runs are---and are not.}
\label{sec:provenance_table}
The 76 runs are \emph{not} 76 same-recipe replication attempts. The SI family-provenance table (SI~Sup.~P) maps each family to the public source it draws on, the known deviation from the original training, the number of runs, the checkpoint-selection rule, and the resulting dev BLEU-4 range. The eight seven-correction runs implement the released config to the maximum extent the artifacts identify, with two disclosed author inferences bracketed by sensitivity runs (\S\ref{sec:faithful_v4}, \S\ref{sec:sensitivity}); the ten step-corrected and four validation-freq-misread runs are partially corrected; the remaining 54 runs are diagnostic, sensitivity, or competence-extrapolation analyses. The public-recipe non-reproduction conclusion rests primarily on the seven-correction family, with the partially-corrected and legacy families as corroborating evidence.



\paragraph{Training-pool readout (descriptive observation).}
\label{sec:readout_def}
We define \emph{training-pool readout} as the in-sample autoregressive free-decode BLEU and exact-match (EM) rate on the full 7{,}060-item training set under beam-3 decoding---not teacher-forced accuracy. The released evaluator reads out its training pool at 78.8 BLEU / 70.7\% EM while retaining dev/test 13.38/12.78; the canonical constructible families read out at 6.6--12.08 BLEU (1.3--2.5\% EM), whereas the unclipped runs exceed the released values (99.98 / 99.9\%). The readout-to-dev ratio (released $\approx$5.9; canonical family 0.93--1.12; unclipped $\approx$6.1) summarises how far a model decodes above its in-domain performance. These are continuous descriptive observations; any threshold is post-hoc.

\subsection{Reconstruction sanity checks and training diagnostics}
\label{sec:sanity_checks}
Three checks on the recipe-conditioned families rule out basic plumbing failures (data loading, masking, decoding alignment, vocabulary mismatch, pose-subsample direction): a small-sample overfit test (50 sequences reach free-decode BLEU 100.00), a unified-inference regression (the released checkpoint reproduces canonical REC 12.78 exactly through one pipeline with byte-identical vocabularies), and the teacher-forced/free-decode split (released: 96.5\% train token accuracy and 78.8 train free-decode BLEU vs.\ defective families' 55--60\% and 7.2--9.3). These checks do \emph{not} rule out subtler training-objective, normalisation, or scheduler defects---indeed seven such defects were later found---so earlier rounds framed RQ1 strictly as ``our disclosed reconstruction did not attain the released competence''. Training logs confirm normal optimisation for the defective families (median train-NLL 5.83$\to$2.09 over epochs 1--25; effective batch 256; a 25$\to$12.5\,fps subsampling regression test passes). Full diagnostics: \texttt{results/robustness\_diagnostics.json}, \texttt{results/fps\_regression\_test.json} (SI~Sup.~R).

\paragraph{Seven implementation defects, and the seven-correction re-implementation.}
\label{sec:protocol_robustness}
\label{sec:faithful_v4}
A line-level audit of our own training scripts identified \emph{seven} defects relative to the released \texttt{config.yaml}'s semantics. The first four were known from earlier rounds; the last three are newly identified here:
\begin{enumerate}\itemsep0pt
\item \emph{CTC loss omitted} (v1) despite \texttt{recognition\_loss\_weight=1.0}; the ten step-corrected runs additionally trained with the recognition weight set to zero.
\item \emph{Validation frequency misread} (v1): \texttt{validation\_freq=14} is 14 optimiser \emph{steps} (JoeyNMT semantics; the released log validates at steps 14, 28, \ldots), not 14 epochs.
\item \emph{Selection branch never saved} (v1): the \texttt{--selection bleu} branch had \texttt{improved} hard-coded \texttt{False}.
\item \emph{Non-autoregressive selection decode} (v1.5/v2): selection BLEU was per-position argmax over teacher-forced logits, not autoregressive decoding; v3 corrected this to beam-3.
\item \emph{Validation mask inversion} (v1--v3): the data loader sets \texttt{txt\_mask=True} at \emph{valid} positions, but token counts (and the v2 greedy truncation length) used the negated mask, counting \emph{padding} positions.
\item \emph{Loss-normalisation mismatch} (v1--v3): the config specifies \texttt{translation\_normalization: batch}; our code used per-token mean CE. The released log pins the \emph{logged} semantics (its step-14 dev \texttt{Translation Loss} decomposes exactly as valid dev tokens $\times\ln\mathrm{PPL}$; SI~Sup.~F), and the training-time reduction (token-sum CE divided by sentences, the JoeyNMT \texttt{batch} reading) is an inference corroborated by train-loss magnitudes and bracketed by sensitivity runs (\S\ref{sec:sensitivity}). Under Adam this changes the effective loss scale by the mean target length ($\sim$15$\times$), altering both the gradient-clipping regime and the relative strength of weight decay.
\item \emph{Stopping-rule mismatch} (v1--v3): a 300-epoch cap with early-stop patience 15. The released log refutes patience-based early stopping outright: the released run improved last at validation \#130 (step 1820, BLEU 13.38) and then ran \emph{72 further non-improving validations}, ending at step 2828 with LR $1.15\times10^{-5}$---an external termination, not a patience trigger. The only stopping condition reproducible from the config is the learning-rate floor (\texttt{learning\_rate\_min}=1e-8), which requires 52 plateau decays of $0.8\times$.
\end{enumerate}

\noindent The seven-correction re-implementation (\texttt{train\_faithful.py}) corrects the seven identified defects and takes every remaining parameter from the released config: seed 42 (the released seed) as the primary run plus seeds 43--49 for within-implementation dispersion; joint CTC$+$translation with batch normalisation; step-level validation every 14 optimiser steps; autoregressive beam-3 selection identical to the evaluation decoder; a plateau scheduler whose 20 decays match the released log's cadence field-for-field; a 3{,}000-epoch cap with the LR-floor stopping rule; and gradient clipping at 1.0 (disclosed as an author inference---the config does not specify clipping; with batch-normalised loss the clip genuinely binds, pre-clip gradient norms over the first ten steps being 377, 568, 145, 33, \ldots, whereas under the earlier token-mean loss it almost never bound). A 56-step smoke run reproduces the released log's step-14 dev token-sum NLL to within 1.1\% (per-token PPL 245.0 vs.\ 259.8, a 5.7\% relative difference; \texttt{results/faithful\_smoke\_gradnorms.json}). The one released-log column we cannot reproduce is the \texttt{Recognition Loss} magnitude: our step-14 dev CTC is 39.9 per sentence, 5.5 per gloss token, 0.74 per input frame, and 20{,}527 as a raw sum, and none of these matches the logged 17.28, so the normalisation of the original CTC term is not identifiable from public artifacts. The two remaining author inferences (CTC normalisation and clip threshold) and the stopping-rule difference are therefore bracketed by sensitivity runs in \S\ref{sec:sensitivity}. The bracketing resolves them asymmetrically: the CTC normalisation proves non-identifiable but mild, whereas the clip threshold proves \emph{load-bearing}---the released config's omission of clip fields, read against the candidate-upstream default of no clipping, makes our 1.0 an eighth defect (\S\ref{sec:sensitivity}, Table~\ref{tab:clip_sensitivity}). In practice each seven-correction run terminated at the plateau scheduler's \emph{numerical} floor (LR frozen at 4.4e-8; torch rejects decay steps smaller than its internal $\varepsilon{=}10^{-8}$), with the best checkpoint unchanged for the final 218--338 validations; the released run itself ended mid-schedule at step 2{,}828, so \S\ref{sec:sensitivity} additionally evaluates checkpoints fixed at the released run's best (1{,}820) and final (2{,}828) steps. The implementation ladder from the defective protocols to the unclipped resolution is SI~Table~S53 (SI~Sup.~V). The SI config-mapping table (SI~Sup.~F) maps every load-bearing field to the original-framework semantics, our v1--v3 semantics, and the seven-correction semantics, with the evidence source for each field (released-log arithmetic, JoeyNMT semantics, config text, or author inference).

\subsection{Discovery timeline and pre-registration status}
\label{sec:discovery}
The $+10.24$ response was first observed on 2026-07-24 (pre-exclusion materialization 2026-07-22). The Jaccard retriever, PURE protocol, and 641-query set were locked before reconstruction training began; six legacy seeds (101--606) were prospectively specified before inspection and are the only prospectively specified follow-up evidence; the eight extension seeds and the headline-cell selection (max-gap among the explored probe cells) were post-hoc, so the headline CI is descriptive. Full provenance in SI~Sup.~C.

\subsection{Metrics and uncertainty}
We report corpus BLEU-4 on the 0--100 sacreBLEU scale \cite{post2018sacrebleu} (sacrebleu 2.5.1; signature in SI~Sup.~Q), with BLEU-1, chrF, ROUGE-L, BERTScore \cite{zhang2020bertscore}, and WER as cross-metric checks (all share decoded outputs) and teacher-forced BT likelihood as a non-decoded control. Uncertainty uses donor-cluster bootstrap (primary; 10{,}000 resamples; 565 unique donors; SI~Sup.~G) and item-level bootstrap; clustering sensitivity for the headline gap (donor/item/two-way/broadcast-date/signer$\times$show) is tabulated in SI~Sup.~G---the 9-cluster signer and 2-cluster broadcast-show designs are exploratory references only, not confirmatory intervals. Seed dispersion is reported as descriptive Student-$t$ intervals (assumptions and leave-one-out checks in SI~Sup.~B); the six primary seeds give $[-2.48,-0.28]$. The headline gap is a \textbf{post-selected descriptive estimate} (\S\ref{sec:probe_multiplicity}); all other contrasts are Holm-corrected within a defined family or labelled descriptive (SI~Sup.~C).

\subsection{Terminology, claim hierarchy, and a claims register}
\label{sec:stratification}
Throughout, \emph{run} denotes a single training execution; \emph{checkpoint} the selected weights from that run; \emph{unique weight binary} counts distinct SHA-256 hashes (one validation-freq-misread seed-202 artifact is byte-identical to a long-schedule artifact); \emph{family} a group of runs sharing a training protocol; \emph{evaluator} a checkpoint used to decode fixed outputs. Claims use four evidence tiers: Descriptive (D; no formal error control), Sensitivity (S; explicit post-hoc condition), prospectively specified within-project follow-up (C; only the six legacy seeds 101--606, seed-level variation rather than independent replications), and Not Identified (NI). Checkpoint families stratify into \emph{Seven-correction} (8 runs, seeds 42--49; primary evidence for RQ1), \emph{Partially-corrected} (14), \emph{Secondary} (14 legacy seeds, 6 prospectively specified), and \emph{Diagnostic} (40); the full registry, family table, and the auto-generated accounting table (76 trained / 74 decoded / 73 unique / 69 non-degenerate, all 69 gaps negative) are in SI~Sup.~D and SI~Table~S26. Table~\ref{tab:claims_register} registers each headline claim with its estimand, unit of analysis, selection status, uncertainty quantification, and evidence tier, so that descriptive, confirmatory, item-level, and seed-level quantities are not read interchangeably.

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{3pt}
\caption{Claims register: estimand, unit, selection status, uncertainty, and evidence tier per headline claim (D descriptive, S sensitivity, C prospectively specified within-project, NI not identified).}
\label{tab:claims_register}
\begin{tabular}{p{2.9cm}p{2.6cm}p{1.3cm}p{2.0cm}p{2.4cm}c}
\toprule
Claim & Estimand & Unit & Selection status & Uncertainty & Tier \\
\midrule
Released probe gap $+10.24$ & PURE$-$REC sacreBLEU under released ckpt & item / donor & post-selected max-gap probe cell & donor-cluster bootstrap (SI~Sup.~G) & D \\
Non-reproduction at clip 1.0 & sign of gap per reconstructed run & run (checkpoint) & families post-hoc; 6 legacy seeds prospective & seed-dispersion interval, LOO folds & D (C for 6 seeds) \\
Signature reproduction unclipped & readout, EM, gap of unclipped runs & run (seed) & threshold grid post-hoc; dev probe held out & 3 seeds per threshold, test and dev & S \\
Reference-frame contrast $9.08$ & gap$_{\text{orig}}-$gap$_{\text{human}}$, 461 matched items & item & post-hoc subset (confidence=1) & paired bootstrap CI & D \\
Pool-origin contrasts $+14.5/+9.6/+8.4$ & SEEN$-$UNSEEN gap under three designs & item / resample & post-hoc & bootstrap / conditional simulation & D \\
Seven-correction competence & dev, test BLEU vs.\ released & run (seed) & seeds fixed (42--49) & range $\pm$ SD over 8 seeds & D \\
Readout--gap association & Spearman $\rho$ within/across regimes & checkpoint & post-hoc panel & no CI (rank statistic) & D \\
Best-seed test 13.11 & max over 8 seeds & run & post-hoc maximum & none (reported, not relied on) & D \\
\bottomrule
\end{tabular}
\end{table}



\section{Results}
\subsection{Adversarial probe response: an attack result, confirmed and localised}
\label{sec:public-diagnostic}
Under the released SLRTP2025 evaluator, source-conditioned whole-donor retrieval scored 23.02 sacreBLEU (0--100 scale), compared with 12.78 for the recorded test poses---a difference of $+10.24$ BLEU points. We present this as the output of the adversarial unit test defined in \S\ref{sec:method}: the number measures the evaluator's susceptibility to this specific lexical-coupling shortcut, not general retrieval quality or signing adequacy. Because the headline cell was selected as the maximal-gap cell among the explored probe cells (SI~Table~S46), we do not attach a confirmatory-looking interval to it; its resampling intervals are reported in SI~Sup.~G as post-selection descriptive quantities.

Three new results characterise the attack precisely. \textbf{(i) Frozen-split confirmation:} the identical probe construction applied to the development split (515 items, never used for probe construction or exploration) reproduces the response at consistent magnitude---REC 13.38 (matching the released training log's 13.38 to three digits, independently validating the decode pipeline) vs.\ PURE 21.47, a gap of $+8.10$ against the test-split $+10.24$ (SI~Sup.~S); the unclipped reconstructions reproduce the same dev-split response at matching magnitude (\S\ref{sec:clip_source}). \textbf{(ii) Text-free selection rules do not reproduce it:} a pose-similarity retriever that never touches text scores 0.45 (gap $-12.33$), indistinguishable from random donors ($-11.88$) and far below text-retrieval TN-PURE (23.02). Because such rules also retrieve less lexically similar donors, they conflate loss of coupling with loss of retrieval quality; the retrieval-query $\times$ scoring-frame grid on the 461-item human-reference subset separates the two (SI~Sup.~T): retrieving with the human back-translations while scoring against the original references removes the response ($-4.02$ $[-5.50,-2.62]$) although the donors still average Jaccard $0.30$ to the originals, whereas re-coupling query and scoring frame on the human texts partially restores it ($+4.29$ $[3.09,5.49]$; original diagonal $+10.03$, crossed cell $+0.95$). Within the tested grid the response coincides with lexical identity of retrieval query and scoring reference, not with text-based selection as such; randomisation within the Jaccard top-$k$ interpolates between regimes ($+7.06$ at $k{=}5$, $+3.34$ at $k{=}20$). \textbf{(iii) Deployment-realistic systems do not show it:} a noisy-gloss retrieval pipeline scores only $-0.13$ relative to REC under the same evaluator (\S\ref{sec:competition_real}).

Further controls (retriever comparison, oracle-gloss and PT-composition baselines, teacher-forced NLL, five complementary text metrics, decoding-parameter sweeps; SI~Table~S32, SI~Sup.~F) localise the effect to source-conditioned donor retrieval: pure donor copy (23.02) exceeded PT-composed (18.86), oracle-gloss (11.4--11.8), PT baseline (1.59), and 20-seed random donors (0.90/0.77); source-text Jaccard was the strongest retriever (LaBSE 19.2, multi-SBERT 16.5); teacher-forced NLL does not reproduce the gap; beam/length sweeps leave the gap at $+9.9$ to $+10.8$.

\paragraph{Probe multiplicity and post-selection status.}
\label{sec:probe_multiplicity}
Before freezing the headline probe we explored four retrieval strategies crossed with three construction variants on the released evaluator alone (6 of 12 cells explored, plus two random baselines; SI~Table~S46) and selected the maximal-gap cell; no multiplicity correction would make a post-hoc selection confirmatory, so the headline gap carries no confirmatory interval. The six prospectively specified legacy seeds (\S\ref{sec:discovery}) provide the only prospectively specified evidence (for the non-reproduction direction, not the gap magnitude).

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{generated_figures/fig2_headline.pdf}
\caption{Adversarial unit-test response under the released evaluator and its absence across the canonical (clip$=$1.0) panel. (a) System scores (sacreBLEU-4, 641-query protocol): TN-PURE 23.02 vs.\ REC 12.78 ($\Delta{=}+10.24$, post-selected), with TN-PTCOMP 18.86, PT 1.59, oracle-gloss 11.4, random-donor 0.90 ($\pm$1 SD over 20 seeds). (b) Per-checkpoint gaps: released ($+10.24$) vs.\ fourteen legacy reconstructions, all negative (donor-cluster bootstrap CIs; dispersion $[-1.85,-0.80]$). The remaining families (union $[-2.80,-0.36]$; full panel $[-2.80,-0.21]$) are in Fig.~\ref{fig:competence}; none is positive, including runs matching the released test-split competence---an absence \S\ref{sec:sensitivity} traces to the shared clip threshold (unclipped: $+11.19$).}
\label{fig:headline}
\end{figure}

The probe reversal motivates the prior question: can the public recipe reproduce the released evaluator at all?

% --- RQ1 precedes the descriptive non-reproduction for the logical dependency between reconstruction and probe response ---

\subsection{RQ1: What the seven-correction re-implementation recovers, and what one inference still hid}
\label{sec:positive_evidence}
Table~\ref{tab:faithful_results} reports the seven-correction family (eight seeds, \S\ref{sec:faithful_v4}) against the released evaluator on four dimensions: dev competence, test-split decoding of the recorded poses (the probe panel's own REC baseline), full-training-pool readout, and the probe gap. Three findings follow; the second and third describe the position \emph{before} the clipping resolution of \S\ref{sec:sensitivity}, which completes the reconstruction.

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{4pt}
\caption{The seven-correction family (\texttt{train\_faithful.py}, seeds 42--49; the seven identified defects corrected) versus the released evaluator. Dev/test BLEU-4 are uniform beam-3 decodes (515/641 items); readout is full-training-pool free-decode BLEU-4 (7{,}060 items) with exact-match rate; gap is the canonical PURE$-$REC sacreBLEU-4 difference on the 641-item test panel. The released evaluator's row uses the same protocols.}
\label{tab:faithful_results}
\begin{tabular}{lrrrrr}
\toprule
Seed & Dev BLEU-4 & Test REC & Train readout (EM\%) & PURE & Gap \\
\midrule
\textbf{42 (released seed)} & 12.05 & \textbf{13.11} & 11.96 (2.4\%) & 10.32 & $-2.80$ \\
43 & 11.59 & 11.60 & 11.45 (2.3\%) & 10.22 & $-1.37$ \\
44 & 11.95 & 12.39 & 11.74 (2.3\%) & 10.13 & $-2.25$ \\
45 & \textbf{12.62} & 12.04 & 11.87 (2.5\%) & 10.08 & $-1.97$ \\
46 & 11.80 & 12.26 & 11.48 (2.3\%) & 10.16 & $-2.10$ \\
47 & 12.03 & 12.53 & 12.08 (2.4\%) & 10.34 & $-2.20$ \\
48 & 12.17 & 12.33 & 11.67 (2.4\%) & 9.98 & $-2.35$ \\
49 & 11.93 & 11.86 & 11.48 (2.4\%) & 9.77 & $-2.09$ \\
\midrule
Released evaluator & 13.38 & 12.78 & 78.8 (70.7\%) & 23.02 & $+10.24$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding 1 (surface competence largely recovered).}
\label{sec:faithful_results}
The seven-correction runs reach dev BLEU-4 11.59--12.62 (primary seed-42 run: 12.05) against the released 13.38---closing most of the gap that the seven defective implementations left open (their ceiling: 10.5). On the test split, where the probe panel itself is scored, the seven-correction evaluators decode the recorded poses at mean 12.27 $\pm$ 0.46 SD (range 11.60--13.11, $n{=}8$) against the released 12.78: the family mean sits 0.51 BLEU below the released evaluator, the best seed (42, the released seed) exceeds it by 0.33, and we report the mean and range---not the best seed---as the reconstruction evidence, since a best-of-8 statistic is optimistically biased. The author-operational competence gate (dev $\geq$ 12.38; \S\ref{sec:sufficiency_def}) is met by one seed (45). The earlier failure to approach the released evaluator's competence is therefore explained \emph{predominantly} by the seven implementation defects; the residual gap and the signature below are explained by the eighth, the clip-threshold inference (\S\ref{sec:sensitivity}), which raises dev to 16.32--17.17 when corrected or relaxed.

\paragraph{Finding 2 (training-pool readout signature blocked by one inference).}
What the seven-correction family does not reproduce is the released evaluator's training-pool behaviour: full-pool free-decode readout is 11.45--12.08 BLEU with 2.3--2.5\% exact match, against the released 78.8 BLEU / 70.7\% EM. The residual gap is therefore concentrated in a \emph{memorisation signature}---near-perfect decoding of the evaluator's own training pool at retained in-domain generalisation---and not in surface competence. Five leakage controls (SI~Sup.~M) rule out teacher-forcing, output--reference misalignment, pose-ignorant lookup, and narrow-text memorisation shortcuts for the released evaluator's readout, so the signature reflects pose-conditioned training-pool exposure. Crucially, this shortfall is not an unrecovered training mechanism: \S\ref{sec:sensitivity} shows it to be a clipping-regime effect that the unclipped default of the underlying framework reproduces (readout 99.98 BLEU / 99.9\% EM across seeds), with the released evaluator's 78.8 / 70.7\% falling between clip 3.0 (53.21/31.8\%) and clip 5.0 (82.69/68.4\%).

\paragraph{Finding 3 (probe response blocked by the same inference).}
All eight faithful evaluators give negative probe gaps ($-2.80$ to $-1.37$; Table~\ref{tab:faithful_results}), including the seed-42 and seed-47 runs whose test REC (13.11 exceeds, 12.53 comes within 0.25 BLEU of, the released evaluator's 12.78). The non-reproduction of the adversarial probe response therefore cannot be attributed to uniformly lower surface competence. Within the clip$=$1.0 constructible family the readout--gap association is in fact \emph{negative} (Spearman $\rho{=}{-}0.56$ over the 37 constructible checkpoints with uniform readouts and canonical gaps; $\rho{=}{-}0.44$ with the released evaluator included), and the released evaluator is the single positive-gap checkpoint of that panel, lying far outside the family's readout range ($6.6$--$12.08$ vs.\ $78.8$). The clipping sensitivity (\S\ref{sec:sensitivity}) resolves the apparent paradox: the readout--gap relation reverses across clipping regimes---strictly negative within the clip$=$1.0 family, strictly positive along the clip ladder (gap $-0.45$ at readout 23.65 for clip 2.0, $+5.04$ at 53.21 for clip 3.0, $+8.05$ at 82.69 for clip 5.0, $+11.19$ at 99.98 unclipped)---so no amount of seed variation at clip 1.0 could have produced the response.

\medskip
\noindent\label{sec:insample}Descriptive triangulation for the constructible family (Fig.~\ref{fig:competence}b,c; SI~Sup.~D): across the 38 constructible checkpoints with uniform readouts, readout is associated with competence within the family ($\rho{=}0.86$--$0.88$) and \emph{negatively} with the gap ($\rho{=}{-}0.56$; $-0.44$ with the released included)---but all 38 share clip 1.0, and \S\ref{sec:sensitivity} reverses the association across regimes (unclipped: readout 99.98, gap $+11.19$), so the within-regime correlation must not be read as ``memorisation antagonises the probe''. No distillation student transfers the readout pattern (SI~Table~S36); the reversal is robust to weight noise near the released checkpoint ($\sigma\leq3\times10^{-3}$; nine fine-tunes), which tests the released weights' vicinity, not an independent construction. Three-view summary: SI~Table~S49.

\paragraph{Leakage checks, distillation, and extrapolation.}
\label{sec:leakage_checks}
Five controls (SI~Sup.~M) rule out four shortcut classes for the 78.8/70.7\% readout---teacher forcing, output--reference misalignment (label permutation collapses BLEU to $\sim$1.0), pose-ignorant lookup (input-side pose permutation collapses BLEU to $<$1; a same-signer held-out control reaches only 12.19), and narrow-text memorisation---consistent with pose-conditioned training-pool exposure. Fifteen distillation students all have negative gaps and transfer no readout pattern (SI~Table~S36); competence extrapolation and per-item decompositions are in SI~Sup.~K.

\paragraph{Synthesis.}
Within the clip$=$1.0 panel, the released evaluator is the only checkpoint combining high training-pool readout (78.8 BLEU, 70.7\% EM) with retained in-domain performance (13.38/12.78); the seven-correction family closes the surface-competence dimension but not the signature. The clipping sensitivity (\S\ref{sec:sensitivity}) then closes the remaining dimension: unclipped training reproduces the signature (readout 99.98 / 99.9\% EM) and a $+10.24$-class probe response ($+11.19$) at even higher in-domain performance (dev 16.32, REC 15.08), with the released operating point's individual coordinates bracketed by different clipping settings rather than matched by one.



\subsection{Sensitivity: stopping rule, CTC normalisation, and gradient clipping}
\label{sec:sensitivity}
The seven-correction protocol differs from the released run in three identified-but-unverifiable respects: stopping point (LR-floor termination vs.\ step 2{,}828), CTC-term normalisation (unidentifiable from the released log; \S\ref{sec:faithful_v4}), and the clip threshold (author inference). Three sensitivity experiments bracket these degrees of freedom; all runs live outside the canonical 76-run panel (separate registry; \texttt{results/faithful\_steps\_eval.json}). The first two leave \S\ref{sec:positive_evidence} unchanged; the third overturns it and resolves the non-reproduction: \emph{the gradient-clip threshold is load-bearing}, and with clipping disabled---the candidate-upstream default when the released config omits clip fields (\S\ref{sec:clip_source})---the reconstruction reproduces both signatures.

\paragraph{Step-matched checkpoints (stopping rule).}
Fresh seven-correction runs (seeds 42--44, identical protocol) save checkpoints at the released run's best (1{,}820) and final (2{,}828) optimizer steps, so the comparison is fixed-step rather than fixed-protocol-termination (SI~Table~S51, SI~Sup.~V). The fresh runs replicate the published family \emph{byte-identically}---the selected \texttt{best.ckpt} files of seeds 42 and 43 have the same SHA-256 hashes as the published runs (e.g.\ \texttt{8e3fba93\ldots} for seed 42), with best dev BLEU-4 12.05 (seed 42, step 2{,}800), 11.59 (seed 43), and 11.95 (seed 44) matching Table~\ref{tab:faithful_results}---so the step-matched checkpoints below are the published trajectories' own states at those steps, and the same-seed retraining doubles as an L4 determinism check on the training pipeline. At the released run's own best step, the reconstructions read out 10.85--11.51 BLEU (2.2--2.4\% EM) with gaps $-1.46$ to $-2.63$; at its final step, 11.32--11.81 BLEU (2.3--2.4\% EM) with gaps $-1.77$ to $-2.49$. Neither the memorisation signature (78.8 / 70.7\%) nor a positive probe response appears at any matched step, so their absence is not an artefact of the longer training horizon or the numerical-floor termination of \S\ref{sec:faithful_v4}; the readout rises mildly with step count within the matched window, an order of magnitude short of the signature.

\paragraph{CTC-term normalisation.}
SI~Table~S54 (SI~Sup.~V) varies the CTC denominator at fixed clip 1.0 (seed 42). None of the four candidates reproduces the released log's step-14 \texttt{Recognition Loss} (17.28; our per-sentence value 39.9, per-gloss-token 5.5, per-frame 0.74, raw sum 20{,}527), so the original normalisation remains unidentifiable from public artifacts. Substantively the choice is mild within the clipped regime: the gloss-token and frame denominators train slightly below the per-sentence default (dev 10.79 and 9.55) and remain signature-free, while the raw-sum variant is degenerate \emph{at clip 1.0} (dev 0.90; the CTC-dominated gradient direction crowds the translation head out of every norm-truncated update). The degeneracy is itself a clipping artefact: with clipping disabled the same raw-sum objective trains healthily (the candidate-upstream run of \S\ref{sec:clip_source}, whose literal \texttt{\_train\_batch} semantics it reproduces).

\paragraph{Gradient clipping is operationally load-bearing in our reconstruction: a dose--response.}
Table~\ref{tab:clip_sensitivity} varies only the gradient-clip threshold (seed 42; the 1.0 row is the seven-correction run of Table~\ref{tab:faithful_results}). The training-pool readout rises monotonically with the threshold---11.96 (2.4\% EM) at clip 1.0, 23.65 (9.0\%) at 2.0, 53.21 (31.8\%) at 3.0, 82.69 (68.4\%) at 5.0, and 99.98 (99.9\%) unclipped---and the probe gap turns positive in lock-step on the test split ($-2.80$, $-0.45$, $+5.04$, $+8.05$, $+11.19$; released $+10.24$) and on the frozen dev probe ($-1.35$, $-1.79$, $+4.14$, $+5.96$, $+8.07$; released $+8.10$), where the two negative cells differ by less than typical seed dispersion and the ordering is monotone from clip 2.0 upward. The dose--response is not a seed-42 artefact: with seeds 43--44 added (three seeds per intermediate threshold), readout, test gap, and dev-probe gap are each monotone in the threshold in \emph{every} seed, and every adjacent threshold pair is separated by more than the between-seed range at either threshold---test gap $-1.11\pm0.63$ (clip 2.0), $+5.39\pm0.69$ (3.0), $+9.28\pm1.07$ (5.0), $+10.94\pm0.55$ (none); readout $23.2\pm0.5$, $56.5\pm4.3$, $92.5\pm8.8$, $98.4\pm2.7$; dev-probe gap $-1.81\pm0.26$, $+3.90\pm0.22$, $+6.85\pm0.76$, $+8.24\pm0.35$ (per-seed values in SI~Table~S55; the seed-43/44 runs terminated at the 1{,}200-validation safety cap, by which point the LR was already frozen at the numerical floor and the best checkpoint unchanged).
Surface competence moves non-monotonically (dev 12.05 at 1.0, 17.17 at 2.0, 16.32 unclipped) and exceeds the released 13.38 for every threshold at or above 2.0. Under Adam with our batch-normalised loss (token-sum CE divided by sentences), the clip threshold effectively rescales the update: it binds hard at 1.0 (pre-clip gradient norms over the first ten steps are 377, 568, 145, 33, \ldots), so clipped runs take uniformly truncated steps and never enter the memorisation regime, whereas unclipped runs memorise the 7{,}060-item training pool essentially exactly while \emph{retaining} dev/test competence (REC 15.08 vs.\ released 12.78). Because clipping was active in essentially every early update, the ladder cannot by itself separate a clipping-regime mechanism from a reduced-effective-step-size mechanism; the claim we make is the operational one---gradient clipping is load-bearing for both signatures in our reconstruction---with the threshold ladder as the mechanism-level clue. The released evaluator's 78.8 BLEU readout falls between clip 3.0 (53.21) and clip 5.0 (82.69)---the post-hoc closest run of the ladder, whose 68.4\% EM the released 70.7\% slightly exceeds---and its $+10.24$ gap between clip 5.0 and no clipping.

\begin{table}[h]
\centering
\scriptsize\setlength{\tabcolsep}{4pt}
\caption{Gradient-clip sensitivity (seed 42; all other parameters the seven-correction protocol). The 1.0 row is the seven-correction run of Table~\ref{tab:faithful_results}; ``none'' disables clipping entirely (\texttt{max\_norm}$=\infty$); ``candidate-upstream'' additionally applies the CTC raw-sum normalisation (the literal signjoey \texttt{\_train\_batch} semantics, commit \texttt{249d3cd}). Protocols as in SI~Table~S54. ``Dev-probe gap'' is the same PURE$-$REC contrast evaluated with the frozen dev-split donor registry (515 queries; \S\ref{sec:public-diagnostic}), which was never used for probe construction or exploration; the released evaluator scores $+8.10$ on it. All rows are seed 42; multi-seed means at the intermediate thresholds are reported in the text.}
\label{tab:clip_sensitivity}
\begin{tabular}{lrrrrrr}
\toprule
Grad-clip threshold & Dev BLEU-4 & Test REC & PURE & Test gap & Dev-probe gap & Readout (EM\%) \\
\midrule
1.0 (seven-correction) & 12.05 & 13.11 & 10.32 & $-2.80$ & $-1.35$ & 11.96 (2.4\%) \\
2.0 & 17.17 & 16.35 & 15.90 & $-0.45$ & $-1.79$ & 23.65 (9.0\%) \\
3.0 & 17.13 & 16.78 & 21.82 & $+5.04$ & $+4.14$ & 53.21 (31.8\%) \\
5.0 & 16.54 & 15.92 & 23.97 & $+8.05$ & $+5.96$ & 82.69 (68.4\%) \\
none ($\infty$) & 16.32 & 15.08 & 26.27 & $+11.19$ & $+8.07$ & 99.98 (99.9\%) \\
candidate-upstream (none, CTC raw sum) & 16.04 & 15.12 & 25.80 & $+10.68$ & $+7.87$ & 97.60 (95.1\%) \\
\midrule
Released evaluator & 13.38 & 12.78 & 23.02 & $+10.24$ & $+8.10$ & 78.8 (70.7\%) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Source evidence: under the candidate upstream semantics, the released configuration implies no clipping.}
\label{sec:clip_source}
The clip threshold was disclosed as an author inference because the released \texttt{config.yaml} specifies no clipping field. The strongest public reading of that absence comes from the upstream framework. The SLRTP2025 evaluation repository releases \emph{no training script}; it states that the evaluation model derives from Sign Language Transformers, and its data pipeline, model definitions, and \texttt{validations.txt} report format correspond field-for-field to signjoey (\texttt{neccam/slt}, commit \texttt{249d3cd}, 2025-04-27; the commit-selection evidence---column-format and code-path correspondence---is in SI~Sup.~U). In signjoey's \texttt{build\_gradient\_clipper}, the config fields \texttt{clip\_grad\_val} and \texttt{clip\_grad\_norm} select, respectively, value clipping and norm clipping (the two are mutually exclusive); when \emph{neither} field is present the builder returns \texttt{None} and no clipping is applied. The released config contains neither field. Under the candidate upstream framework semantics, the released configuration therefore implies \emph{no clipping}---the regime of the ``none'' row---and the behavioural reconstruction is consistent with this interpretation. What the public artifacts cannot establish is that the original training used exactly this framework version with no local modifications: no training transcript or organiser confirmation is available, so the attribution is conditional (``under the candidate upstream semantics''), not transcript-confirmed. The literal candidate-upstream training semantics---no clipping \emph{and} the CTC raw sum (Sup.~U)---give the candidate-upstream row, whose probe gap ($+10.68$) is the closest single reproduction of the released $+10.24$. Our 1.0 threshold---imported from the v1--v3 scripts and disclosed as an inference---was therefore an \emph{eighth} defect in our reconstruction, and a load-bearing one: it is the single choice that hid both the readout signature and the probe response, and it did so invisibly, since every metric visible during training (dev BLEU, NLL, CTC) looks healthy at clip 1.0.

\paragraph{Step-matched unclipped readout, multi-seed and frozen-dev confirmation.}
The result is not a seed-42 accident or a training-duration artefact. Across seeds 42--44, unclipped runs give test-split gaps $+11.19$, $+11.31$, and $+10.31$ with readouts 99.98, 95.25, and 99.98 BLEU (90.5--99.9\% EM) at dev 15.74--16.32. It also holds on the \emph{frozen dev probe}---the 515-query donor registry constructed identically to the test probe but never used for probe construction or exploration (\S\ref{sec:public-diagnostic}): the three unclipped seeds score gaps $+8.07$, $+8.00$, and $+8.64$ and the candidate-upstream variant $+7.87$, against the released evaluator's $+8.10$ on the same registry (\texttt{results/dev\_probe\_eval.json}); the unclipped regime reproduces the probe response on a split that was never explored when the response was found. In a fresh unclipped run with checkpoints fixed at the released run's own steps, the readout is already 99.94 BLEU / 99.7\% EM at step 1{,}820 (dev 15.69, gap $+11.68$) and 99.98 / 99.9\% at its final step 2{,}828---the memorisation regime is entered \emph{before} the released run's best checkpoint, so the overshoot relative to the released 78.8 is not an artefact of training longer than the released run. What remains unreproduced is the released operating point itself: no single knob places dev 13.38, readout 78.8, REC 12.78, and gap $+10.24$ simultaneously---clip 5.0 (post-hoc, selected after inspecting the ladder) matches the readout (82.69/68.4\%) but overshoots dev by 3.2, while clip 1.0 matches dev-range but not the signature---so at least one further quantitative difference (CTC normalisation, stopping point, framework version, or data order) persists: the released point's \emph{individual coordinates} are bracketed by different clipping settings, and the point itself is not matched.

\paragraph{Three levels of reconstruction.}
\label{sec:three_levels}
The sensitivity results support three statements of increasing strength, and we claim only the first two. \emph{(i) Qualitative signature reproduction} (supported): an implementation of the released recipe with the clip inference removed produces both previously unreproduced properties---high training-pool readout and a positive probe response---across seeds and on a frozen, never-explored split. \emph{(ii) Partial numerical bracketing} (supported): the released evaluator's individual coordinates (dev, REC, readout, gap) each fall inside the range spanned by the clip ladder, but no single threshold matches all of them simultaneously within any pre-specified tolerance, so the released operating point is bracketed coordinate-wise, not reconstructed. \emph{(iii) Mechanism confirmation}---that the original training in fact ran unclipped---is \emph{not} established: it rests on the candidate-upstream source semantics plus behavioural consistency (\S\ref{sec:clip_source}), and would require a released training transcript or organiser confirmation.

\subsection{Descriptive observation: probe non-reproduction under the clip$=$1.0 panel, and its resolution}
\label{sec:what-drives-reversal}
Across every constructible evaluator of the canonical panel---eight seven-correction runs, fourteen partially-corrected runs, fourteen legacy seeds, and the diagnostic families, all sharing the clip$=$1.0 inference---the probe ordering does not reproduce: all 69 non-degenerate decoded runs have strictly negative gaps (range $[-2.80,-0.21]$; 0 positive). For the fourteen legacy reconstructions the within-implementation seed dispersion interval is $[-1.94,-0.46]$ (mean $-1.20$, SD 0.33; the six prospectively specified seeds give $[-2.48,-0.28]$, below zero in all leave-one-seed-out folds), and the original-minus-run difference-in-differences estimates range from $+11.04$ to $+12.09$ (SI~Table~S2), every interval excluding zero; the seven-correction family shows the same sign at substantially higher competence ($[-2.80,-1.37]$). Because those runs reach the released evaluator's test-split decoding competence while lacking its training-pool signature, the non-reproduction is not attributable to surface competence; \S\ref{sec:sensitivity} attributes it to the one hyperparameter all 69 runs share: disable clipping and the same recipe produces readout 99.98 / 99.9\% EM with a $+11.19$ gap (three seeds). The 69-run uniformity is thereby reinterpreted as robustness \emph{under a wrong inference} shared by every run---the runs are coverage evidence, not independent replications---and the probe response as constructible after all. Competence-gate details and pass/fail table: SI~Sup.~D--E (one of 60 gate-eligible runs passes; the conclusion is threshold-insensitive across the reported grid).

\paragraph{What the released training log establishes.}
\label{sec:original_trajectory}
The bundle's \texttt{validations.txt} logs dev BLEU-4 reaching 13.38 at step 1{,}820 (Fig.~\ref{fig:competence}a); the defective families plateau at 8--10 and the seven-correction family at 11.6--12.6, so within the canonical panel the final 0.76-BLEU ascent and the readout signature are not explained (\S\ref{sec:faithful_results}); the clipping sensitivity (\S\ref{sec:sensitivity}) attributes both to the clip threshold. Input degradation (temporal downsampling) compresses both scores but does not eliminate the reversal (at $k{=}8$: REC 8.75, PURE 11.69, gap $+2.94$; SI~Sup.~F).

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{generated_figures/fig3_competence.pdf}
\caption{Competence and training-pool readout separate the released evaluator from every canonical (clip$=$1.0) checkpoint. (a) Dev-BLEU-4 trajectories on a common step axis: released (orange) reaches 13.38 at step 1{,}820 of 2{,}828; defective-protocol reconstructions (blue band) plateau at 8--10, the eight seven-correction runs (red band) at 11.6--12.6. (b) Gap vs.\ dev BLEU-4 by family (markers per legend; released star); the gray band spans the constructible 6.5--12.6 range; the $(12.62,13.38)$ interval is unobserved by recipe-constructed checkpoints (the nine fine-tunes there are weight perturbations); degenerate checkpoints hollow. (c) Full-pool readout vs.\ dev BLEU-4: canonical checkpoints read out at most 12.1 BLEU; the released evaluator 78.8 (70.7\% EM) at dev 13.38; the unclipped run 99.98 (99.9\% EM) at dev 16.32. Families are descriptive; checkpoints are not IID replicates.}
\label{fig:competence}
\end{figure}

The gap is positive across every decoded-text metric under the released evaluator (BLEU-1 $+20.94$, chrF $+12.93$, ROUGE-L $+17.87$, WER $+13.90$ sign-flipped) and negative across all metrics under the six prospectively specified reconstructions, while teacher-forced NLL runs the other way (SI~Table~S32, SI~Sup.~H rank stability).

\paragraph{Reference-frame sensitivity: matched-subset analysis.}
\label{sec:human_refs}
\label{sec:floor_effect}
Two 2026 corpus audits bear directly on this reversal. Czehmann et al.~\cite{czehmann2026sacred} released manual sign-to-text back-translations of the test set (one deaf fluent L2 DGS signer; per-item confidence flags), documenting information loss, lexical errors, and translationese in the original speech-transcribed references, and showed that reference replacement already changes BLEU scores and rankings on PHOENIX-2014T. Alkain et al.~\cite{alkain2026critical} quantified train--test text overlap across sign-language translation corpora, showing that a small fraction of highly training-like test items disproportionately inflates corpus BLEU. We reproduce the overlap pattern locally: the 32 test items whose reference exactly matches a training text score REC 40.26 / PURE 78.73 (gap $+38.5$), while the remaining 609 items score REC 11.93 / PURE 22.20 (gap $+10.3$).

Czehmann et al.\ compared original- and human-reference conditions on the same confidence=1 subset (462 of 642 items); we follow the same protocol. On the matched 461-item subset (after join with our 641 released test IDs), the original-reference gap is $+10.03$ BLEU points (REC 14.94 / PURE 24.98) and the human-reference gap is $+0.95$ (REC 7.35 / PURE 8.30): a \emph{reference-frame contrast} of $9.08$ BLEU points (bootstrap CI $[7.44, 10.86]$; the ratio form, a 90.5\% gap reduction, is reported only in SI~Table~S41 as a descriptive quantity; SI~Fig.~S5; per-item paired view in SI~Fig.~S2). The interval for the residual gap includes zero (paired item CI $[-0.13, 1.99]$). This contrast is a \emph{single-annotator, 461-item, post-hoc association}, not an intervention in the experimental sense and not a reference-defect effect: reference replacement simultaneously changes (i) reference content, (ii) expression style/translationese, (iii) BLEU floor level, and (iv) the lexical coupling between retrieval selection and scoring reference. Teacher-forced BT likelihood does not reproduce the probe response under either reference (REC NLL $3.060$ vs PURE $3.479$ on original refs; $3.41$ vs $3.71$ on human refs) \cite{jiang2025meaningful}.

Scoring against both references jointly leaves the gap at $+11.50$, \emph{larger} than original---adding the original reference injects a second lexically coupled target---so the response is specific to scoring frames lexically coupled to the retrieval query (full grid in SI~Sup.~T). Under all six prospectively specified reconstructions the human-reference gap is negative ($-0.15$ to $-0.65$). The asymmetric PURE-vs-REC drop is numerically inconsistent with simple symmetric floor compression, though BLEU's non-linearity precludes a definitive test (SI~Table~S46). The human back-translations are sign-conditioned paraphrases of the recorded test poses, not judgments of the replay donors, so semantic adequacy of TN-PURE-v1 is unmeasured.

\subsection{Donor-origin contrast}
\label{sec:exposure_results}
Restricting retrieval to unseen poses inverts the response: UNSEEN-PURE-v1 (640 other test poses) scores 8.51 vs.\ REC 12.78 (gap $-4.27$; unmatched pool-substitution contrast $+14.51$, SI~Table~S40). Because SEEN and UNSEEN differ in pool size, donor--reference Jaccard, donor reuse, and exposure at once, three designs bracket the confounding: Jaccard-caliper matching (148 items, SMD 0.065) leaves $+9.56$ $[+7.47,+11.76]$; pool-size-controlled resampling (1{,}000 draws of 640-donor pools) leaves $+8.38$ $[+7.44,+9.34]$; and a 605-query seen/unseen factorial decomposes the unmatched contrast into a pool-origin main effect $+2.08$ $[+1.56,+2.57]$, a similarity effect $+6.96$ $[+5.93,+8.08]$, and an interaction $+3.70$ $[2.63,+4.79]$ (Holm $p{=}0.0003$; across reconstructions the seen main effect shrinks to $+0.51$). All three remain associations, not identified training-exposure effects (three different target populations; SI~Sup.~E).

\subsection{Competitive-system context}
\label{sec:competition_real}
Because the audit's adversarial probe is constructed rather than competitive, the public SLRTP2025 leaderboard offers external context: the ``Ground Truth'' row reproduces our canonical REC numbers exactly (BLEU-4 12.78, \cite{slrtp2025}; SI~Table~S35). The retrieval-based winner USTC-MoE sits roughly even with REC on lexical BT metrics (BLEU-4 $-0.72$; SI~Table~S35). We re-decoded two realistic retrieval outputs under all available evaluators (SI~Sup.~P): the deployment-realistic noisy-gloss retrieval pipeline decodes at 12.65 under the released evaluator (gap $-0.13$ vs.\ REC), and our rebuild of the winner's fixed outputs from its released intermediates decodes at 2.42 on the 497 keys those intermediates cover (gap $-9.94$ against the same-subset REC of 12.36; a failed reproduction, not representative of the original entry). The noisy-gloss system was cross-evaluated under the 69 unique non-degenerate reconstructed evaluators of the \emph{cross-evaluation panel}: 75 cross-evaluation decodes (the 74-decode canonical gap panel plus \texttt{rescue\_wd0}, which has no canonical gap; the never-cross-evaluated \texttt{sf\_seed\_1809} excluded), minus five degenerate runs, with the byte-identical \texttt{cf\_202}/\texttt{ls\_202} pair counted once---including the eight seven-correction runs, and gives negative gaps throughout (range $[-3.22,-0.07]$; seven-correction sub-range $[-3.22,-1.59]$); USTC-MoE was cross-evaluated under the same panel minus the seven-correction family (62 non-degenerate evaluators) and remains deeply negative ($[-9.52,-4.28]$). The $+10.24$ gap is specific to the constructed adversarial probe; archiving fixed per-team outputs alongside public scores is the most direct suggestion we can offer for enabling rank-stability audits.

\section{Discussion}
The audit yields a sharpened picture of what the SLRTP2025 public artifacts reproduce, organised around RQ1 (reconstruction), RQ2 (reference-frame and donor-origin sensitivity), and one descriptive observation (probe non-reproduction and its resolution).

\emph{RQ1---Competence recovered first, the signature second; both were our defects.} After seven implementation defects were identified and corrected (\S\ref{sec:protocol_robustness}), the seven-correction family reaches dev BLEU-4 11.59--12.62 against the released 13.38 (test family mean 12.27 $\pm$ 0.46 vs.\ 12.78). The competence gap of the earlier ``failed reconstruction'' was thus attributable predominantly to defects in our own code---four of the seven invisible without forensic arithmetic against the released training log. The training-pool signature initially appeared to be the genuine residual; the clipping sensitivity (\S\ref{sec:sensitivity}) closes it as well: unclipped training reproduces the signature and probe response, so the eighth defect was a wrong hyperparameter inference whose effect was invisible in every training-time metric. What remains unreproduced is the released operating point itself: different clipping settings bracket its individual coordinates, but no single threshold matches all coordinates jointly, and the canonical panel's residual dev interval $(12.62,13.38)$ is exceeded, not filled, by the sensitivity runs.

\emph{Descriptive observation---probe non-reproduction was real, uniform, and wrong as an attribution.} All 69 non-degenerate evaluators of the clip$=$1.0 panel give negative gaps, robustly across seeds and families; the non-reproduction could not be attributed to surface competence, yet attributing it to an unrecovered training mechanism was also wrong---one hyperparameter, shared silently by all 69 runs, was responsible. Two morals: uniformity across many runs is not evidence of artifact insufficiency when the runs share an untested inference, and the two signatures are jointly constructible properties of the released recipe, co-occurring in the unclipped regime and in the released evaluator, with the within-regime readout--gap association ($\rho{=}{-}0.56$) reversing across the ladder.

\emph{RQ2---Reference-frame and donor-origin sensitivity.} On the matched 461-item confidence=1 subset of Czehmann et al.'s~\cite{czehmann2026sacred} human back-translations, reference replacement is associated with a gap reduction to $+0.95$ from $+10.03$---a single-annotator reference-frame contrast that cannot isolate reference-text defects from lexical-coupling disruption, paraphrase shift, or BLEU floor effects. The matched donor-pool analysis (\S\ref{sec:exposure_results}) shows the pool-origin association persists at $+9.56$ under SMD$<$0.1 Jaccard matching; the pool-size-controlled resampling (640-vs-640, \S\ref{sec:exposure_results}) gives $+8.38$ (conditional simulation interval $[+7.44,+9.34]$). All three pool-origin estimates remain associations, not identified training-exposure effects.

\paragraph{Relation to prior work.}
These findings extend prior critiques of BT validity from output faithfulness to public-artifact reproducibility: improved BT scores need not imply target conditioning \cite{hong2026conditional}, low recorded-pose BT baselines are documented \cite{walsh2024datadriven}, and BT likelihood is a more stable correlate of human judgment than decoded BLEU \cite{jiang2025meaningful}. Our reversal is a \emph{decoded-sacreBLEU} reversal (teacher-forced likelihood does not reproduce it), and whole-donor replay is a constructed probe, whereas SOKE-style word-level retrieval can improve pose quality under a different representation \cite{soke}. Chen et al.'s~\cite{chen2022repro} reproduction failure of a released metric has the same anatomy we document for a full evaluator: an eval-only release, underspecified training semantics, and reconstructions that fail until the semantics are pinned forensically.

\paragraph{Scope limitation: our reconstruction vs.\ public-artifact sufficiency.}
\label{sec:scope_limit}
Our conclusion is strictly about what our disclosed implementations construct from the public artifacts; three caveats bound the sufficiency claim. First, the released operating point is bracketed coordinate-wise, not matched, and the CTC normalisation remains unidentifiable, so residual quantitative differences of unknown sign persist. Second, the clipping resolution rests on candidate-upstream source semantics (absent fields $\Rightarrow$ no clipping), an inference about the released run's framework version rather than a released training transcript. Third, all constructions share our own data pipeline. The earlier, stronger negative claim (``the artifacts do not reproduce the signature'') is withdrawn with the disclosure of the eighth defect.

\paragraph{Value, finding, and conclusion reproducibility.}
Following the tripartite framework of \cite{cohen2018three}: \emph{values} recompute (REC 12.78, PURE 23.02, gap $+10.24$ regenerate from the canonical registry); \emph{findings} reproduce across all three dimensions (surface competence; readout signature and probe response unclipped), with the released operating point bracketed coordinate-wise, not matched; \emph{conclusions} about artifact sufficiency hold operationally, subject to the caveats of \S\ref{sec:scope_limit}.

\paragraph{Actionable recommendations for benchmark organizers.}
The audit yields three transferable lessons, scoped to the constructed replay-probe stress case on PHX-public rather than to general SLP leaderboard stability.

\emph{Lesson 1: an undocumented framework default can masquerade as an unreproducible property.} Because the released \texttt{config.yaml} specifies no clip fields, the framework default (no clipping) silently governed whether our reconstruction memorised its training pool and responded to the replay probe---and nothing visible during training distinguishes the regimes. Sixty-nine runs sharing our wrong 1.0 inference uniformly hid both properties. Benchmarks should release the exact training command and seeds, pipeline scripts beyond the manifest, a SHA-256 manifest of training tensors, intermediate checkpoints, and fixed per-team pose outputs: even released numbers under-determine the code that produced them.

\emph{Lesson 2: on template-dense benchmarks, retrieval entries may benefit from donor-exclusion disclosure.} A donor-exclusion sweep is cheap and exposes the reference-coupling gap; the training-pool readout ratio (released $\approx5.9$; clip-1.0 family 0.93--1.12; unclipped $\approx6.1$) and input-side pose permutation (SI~Sup.~M) are two informative probes, with generalisation to other benchmarks untested.

\emph{Lesson 3: reference-coupling artifacts warrant a supplementary reference check.} The reference-frame contrast is a single-annotator case study; template-dense benchmarks could adopt \emph{multi-annotator} back-translations as a supplementary reference set and flag systems whose gap shifts between frames.

\paragraph{Limitations.}
The audit is confined to PHX-public, a template-dense weather domain with documented reference-validity and train--test-overlap issues~\cite{czehmann2026sacred,alkain2026critical}; a 2026-07-14 host compromise required post-event retraining of most checkpoint families under identical protocols, with five unrecoverable early families omitted (Data availability; SI~Sup.~D). Per-team pose outputs are not archived; our cross-evaluator studies decode at or below REC under all 69 canonical non-degenerate evaluators (all clip 1.0). The seven-correction family terminates at the plateau scheduler's numerical LR floor (4.4e-8) rather than the configured floor (1e-8), with best checkpoints unchanged for the final 218--338 validations; the released run itself ended mid-schedule at 101 epochs, so the two stop points are not identical---\S\ref{sec:sensitivity} evaluates checkpoints fixed at the released run's best and final steps and finds the conclusions unchanged. The clipping resolution rests on signjoey source semantics for absent config fields and is confirmed behaviourally, not by a released training transcript; the CTC-term normalisation remains unidentifiable from public artifacts, and the sensitivity grid of \S\ref{sec:sensitivity} brackets both. The unclipped reconstruction overshoots the released operating point (readout 99.98 vs.\ 78.8; dev 16.32 vs.\ 13.38), so the exact released configuration is bracketed, not recovered; the unclipped variant is confirmed across three seeds (gaps $+11.19$/$+11.31$/$+10.31$), clip 2.0, 3.0, and 5.0 are replicated across three seeds (Table~\ref{tab:clip_sensitivity} shows seed 42; means $\pm$SD and per-seed values in the text and SI~Table~S55), and the candidate-upstream variant is single-seed. The human back-translations are a single-annotator resource without second-annotator adjudication; the reference-frame contrast is conditional on one signer's interpretation (qualitative direction stable across confidence tiers; SI~Table~S41). The probe response is a decoded-BT-text-metric effect, not a sign-language quality judgment; without target-conditioned DGS signer evaluation, semantic adequacy of the donor motion is unmeasured---``evaluator readability'' and ``signing quality'' are distinct constructs, and this audit measures only the former.

\section{Conclusion}
This audit reports what a best-effort, artifact-conditioned reconstruction of the released SLRTP2025 BT evaluator from public artifacts reproduces, and what stood in the way. Correcting seven implementation defects in our reconstruction recovered most of the evaluator's surface competence (dev BLEU-4 11.59--12.62 vs.\ 13.38; test decoding family mean 12.27 $\pm$ 0.46, best seed 13.11, vs.\ 12.78). The two properties that then still failed to reproduce across 69 constructible runs---the training-pool memorisation signature (78.8 BLEU / 70.7\% exact match vs.\ a constructible ceiling of 12.08 / 2.5\%) and the adversarial probe response ($+10.24$ vs.\ $[-2.80,-0.21]$)---proved to be blocked not by an unrecovered mechanism but by an eighth defect: our gradient-clip threshold of 1.0, imported as an author inference, whereas under the candidate upstream framework semantics the released config's absent clip fields imply no clipping (\S\ref{sec:clip_source}; conditional on that framework version, not transcript-confirmed). Unclipped, the same recipe reproduces both properties (readout 99.98 BLEU / 99.9\% exact match; probe gap $+11.19$) at dev 16.32, with the released operating point's coordinates bracketed by different clipping settings; the readout--gap association is regime-dependent (negative among clipped checkpoints, positive across clipping regimes). The probe itself behaves as an adversarial unit test: the response was observed on a never-explored dev split under the released evaluator ($+8.10$) and, at matching magnitude, under the unclipped reconstructions ($+8.07$/$+8.00$/$+8.64$); it was not observed under any text-free donor-selection rule tested (pose-signature retrieval: $-12.33$), was observed only on the coupled diagonal of the retrieval-query $\times$ scoring-frame grid, and was not observed for deployment-realistic systems. The audit's lasting contributions are the forensic method (pinning released-config semantics by arithmetic against the released training log, and framework source defaults against config absences), the provenance, checkpoint-registry, and evaluator-audit infrastructure, the dimension-by-dimension reproducibility accounting, and the cautionary mechanism it documents: an undocumented framework default can make a reproducible property look unreproducible---and can do so across sixty-nine runs at once when, as here, they silently share the same wrong inference.

\section*{Statements and Declarations}
\label{sec:ethics}
\paragraph{Funding.} The authors declare that no funds, grants, or other support were received.
\paragraph{Conflict of interest.} The authors declare no competing interests.
\paragraph{Data governance and ethics.} The datasets and model weights are available under research-specific terms: PHOENIX-2014T under its RWTH Aachen academic licence; the SLRTP2025 pose bundle and released BT evaluator under challenge terms; the Czehmann et al.~\cite{czehmann2026sacred} back-translations under CC BY-NC-SA 4.0; CSL-Daily under provider-specific academic terms. The artifact repository is a licence composition: the root MIT Licence covers only the RCP audit software; per-directory LICENSE/NOTICE files document the remainder (including ShareAlike obligations for the Czehmann CSVs). Pose tensors retain signer identity and biometric features; we release per-item decoded text (low re-identification risk) and a curated subset of trained checkpoints, but not per-signer raw poses. The original consent forms are not publicly available, so we cannot independently confirm that informant consent covers derivative replay-probe analysis; no new human-subjects data was collected. No Deaf/DGS stakeholders were involved in formulating the research questions or interpreting results: the adversarial-probe framing, reference-frame analysis, and recommendations reflect a hearing-researcher perspective on a DGS resource, and Deaf-led sign-language AI research norms should guide any follow-up~\cite{desai2024systemic}. The Czehmann et al.\ back-translations we build upon were produced by a Deaf fluent L2 DGS signer.

\paragraph{Data availability.} All numerical values, per-checkpoint data, decoded hypotheses, and analysis scripts are in the artifact at \url{https://anonymous.4open.science/r/rcp-audit-artifact-B314/README.md}. The artifact is version-controlled (git commit \texttt{30fbd00}, 2026-08-19); a claim-to-file manifest mapping each headline number to its source file and verification command is at \texttt{claim\_manifest.json} (artifact root), and a single-command consistency check (\texttt{make check-consistency}, invoking \texttt{scripts/check\_paper\_consistency.py}) verifies the paper's counts against the disk-scanned registry. The L1 audit environment is captured by \texttt{requirements-audit.txt} (4 packages: numpy, scipy, sacrebleu, pyyaml; install via \texttt{make install-audit}); the full \texttt{requirements.lock.txt} (236 packages) is needed only for L3 re-decoding and L4 from-scratch retraining. A persistent DOI-bearing Zenodo release is deposited at \url{https://doi.org/10.5281/zenodo.21987786}, available at submission and not deferred to acceptance. \emph{Artifact integrity note:} the audit workstation suffered a root-level host compromise on 2026-07-14 that was cleaned the same day; the released evaluator bundle, the SLRTP2025 raw dataset, the decoded BT cells, and the paper sources were unaffected and are hash-verified in the artifact, while most trained checkpoints were rebuilt from scratch after the event under identical protocols (byte-identical determinism check in \S\ref{sec:sensitivity}); five unrecoverable early checkpoint families are omitted from the disk-scanned registry, and the full recovery history is documented in the artifact README and SI~Sup.~D. The supplementary material (\texttt{supplementary.pdf}) contains all SI appendices. Upstream datasets are available under research-specific terms documented in the artifact README and per-directory LICENSE/NOTICE files (PHOENIX-2014T under RWTH Aachen academic licence; Czehmann et al.\ human back-translations under CC BY-NC-SA 4.0, redistributed with attribution under those terms; CSL-Daily and SLRTP2025 under provider-specific academic terms).
\paragraph{Author contribution.} Both authors contributed to the study conception, design, data analysis, and manuscript preparation, and both read and approved the final manuscript. Specific contributions (CRediT taxonomy): \textbf{Xin Ji}: conceptualisation, methodology, software, investigation, formal analysis, data curation, writing---original draft, visualisation. \textbf{Cheng Zhang}: methodology, supervision, writing---review and editing, project administration.
\appendix
\section*{Supplementary Information (SI)}
\addcontentsline{toc}{section}{Supplementary Information}
The following supplementary materials, referenced throughout the main text, are available as a separate file (\texttt{supplementary.pdf}) and in the anonymous artifact. Each appendix is annotated with the question it answers.
\footnotesize \textbf{Sup.~A} Missing Test ID Sensitivity; \textbf{Sup.~B} Per-seed Legacy and Extension Cells; \textbf{Sup.~C} Analysis Provenance and Multiplicity; \textbf{Sup.~D} Canonical Checkpoint Registry; \textbf{Sup.~E} Donor-Pool Substitution Decomposition; \textbf{Sup.~F} Config-Mapping and Perturbation Details (incl.\ the four-column config-mapping table); \textbf{Sup.~G} Cluster-Aware Bootstrap; \textbf{Sup.~H} Supplementary Oracle Diagnostics; \textbf{Sup.~I} Template-Density, Holdout Boundary, Donor-Exclusion; \textbf{Sup.~J} Transcript-Slot Diagnostic; \textbf{Sup.~K} Competence Extrapolation and Per-Item Gap Decomposition; \textbf{Sup.~L} CSL-Daily Boundary Control; \textbf{Sup.~M} Leakage Sanity Checks; \textbf{Sup.~N} Pre-Exclusion Materialization Sensitivity; \textbf{Sup.~O} BLEURT-Substitute Validation; \textbf{Sup.~P} Main-Text Tables (moved from the main text); \textbf{Sup.~Q} Additional Protocol Details; \textbf{Sup.~R} Reconstruction Sanity Checks; \textbf{Sup.~S} Frozen-Split Confirmation and Reference-Free Retrieval (incl.\ the full probe-multiplicity matrix); \textbf{Sup.~T} Retrieval-Query Decoupling Grid; \textbf{Sup.~U} Gradient-Clipping and CTC-Normalisation Sensitivity; \textbf{Sup.~V} Round-34 Additions (split reconciliation, frozen-dev probe panel, multi-seed clip ladder, tables relocated from the main text). Each appendix is annotated with the question it answers; labels in \texttt{supplementary.pdf} match the references throughout the main text.

Full checkpoint registry in SI~Sup.~D.

\bibliography{references}

\end{document}
